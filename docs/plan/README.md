# Development Plan — ops v2

**The living document.** Every session reads this file first and updates it
last. If a milestone moved and this file did not, the work is not finished.

Started 2026-09-10. Direction changed the same day — see "How we build" below.

Language: English, because this is the document the owner and Claude work
from. **Domain vocabulary stays verbatim** — status strings, id formats,
account spellings and Indonesian terms are copied exactly from
`00-context.md`. They are data, not prose; translating them breaks things.

---

## How we build: frontend first, demo data, deployed

**Phase 1 (now, D1–D14): the whole workflow as a working frontend, on demo
data, live on Vercel.**

No database. No Supabase project. No backend. Every screen is real, every
button works, every state transition happens — against demo data that lives
in the browser. The URL is shareable on day one and updates on every push.

**Why this order.** The stated problem is that the business rules and
requirements were never specified up front. You cannot specify them in the
abstract, and you cannot discover them from a schema. You discover them by
walking the workflow and finding the place where the screen cannot answer a
question. So: build the workflow, walk it, write down what it teaches, and
only then commit to a schema.

The consequence that matters: **the schema stays free.** Nothing is migrated,
nothing is seeded, nothing has to be backfilled when we learn on day nine
that an approval needs a threshold. Changing the demo data is editing a
TypeScript file.

**Phase 2 (after Phase 1 is walked and signed off): the real backend.**
The database design in `02-database.md` and the API design in `03-api.md` are
already written, and Phase 1 is built **against those contracts**. The demo
data layer implements the same function signatures, the same response
envelope, and the same refusal codes the real API will return. Swapping it is
changing one module — not rewriting screens.

---

## Milestone board

Status: `TODO` · `WIP` · `DONE` · `BLOCKED` · `PARKED`
Update the row, the date, and the note **in the same commit** as the work.

### Phase 1 — the workflow, on demo data, on Vercel

| # | Milestone | Day | Status | Updated | Note |
|---|---|---|---|---|---|
| M0 | Plan and design documents | D1 | DONE | 2026-09-10 | this document set |
| M1 | Demo data layer + contracts | D1 | DONE | 2026-09-11 | deployed. Preview builds on every push to this branch |
| M2 | Demo session: multi-module grants + named authorities | D2 | DONE | 2026-09-11 | grant picker; menu filtering verified in a browser |
| M3 | Reference data: vendors, items, projects | D3 | DONE | 2026-09-11 | curation, merge, two-price model; verified in a browser |
| M4 | Requests board: line-first, four meeting states, evidence | D4 | DONE | 2026-09-11 | redefined per owner — line is the unit (D48) |
| M4b | Approved-against-paid: variance, explanation, pay-from-line | D4 | DONE | 2026-09-11 | D53–D57. Posting a payment from a line writes the ledger row (D32 delivered early) |
| M5 | PR: approval — decided on the meeting board, trail on the line | D5 | DONE | 2026-09-11 | D74. Checkbox + reducible qty and amount + instructions/remark + trail + un-approve |
| M5b | Approval asked for and answered in Google Chat | D5 | DONE | 2026-09-11 | D69–D72. Sent as a batch with its totals; the identity on the record is the approver's, not the meeting laptop's. `/demo/chat` stands in for the signed webhook |
| M5c | Meeting board: decide, fund, add on the spot | D5 | DONE | 2026-09-11 | `/procurement/meeting` (D73, D74). The transfer into BCA 271 is the number it exists for |
| M6 | Payment rounds: sync, approve, transfer, close | D6 | DONE · PARKED | 2026-09-11 | `/procurement/rounds`. Funding needs proof (D80) and comes two ways (D81). A funded round still pays nobody — proved on screen (A10). **Parked (D105)** — kept and working, no further work; the meeting board and the tracker already answer what it was for |
| M7 | **Checkpoint: walk Flow B on a phone, with the team** | D7 | DONE | 2026-09-11 | walked with the owner across this session; the corrections it produced are D64–D83 and F15–F22 |
| M8 | Ledger: list, drawer, attach from a row, void, complete | D8 | DONE | 2026-09-11 | `/accounting/ledger` + cash position, pagination, posting with evidence and detail (D84–D89) |
| M9 | Evidence attached from the record — the main road | D9 | DONE | 2026-09-11 | one `<EvidenceStrip>` in both drawers, *also covers*, the money-to-document path, camera capture. The documents browser is parked (D93, restated by the owner in D105) |
| M10 | The exception inbox: bought first, approved later | D10 | DONE | 2026-09-11 | `/accounting/verifikasi`, five roads, nothing discarded. Reject is the road that never reaches the ledger (D94) |
| M11 | Purchase tracker: obligations across every vendor, two axes per supplier, receiving with both documents | D11 | DONE | 2026-09-11 | `/procurement/tracker` — total obligations at the top, settled vendors split off (D102), a full-width page per supplier (D103) with orders, payments, deliveries and *billable now* (D97–D99). Orders placed from here (D100); a delivery needs photo **and** tanda terima, receiver and QC (D101) |
| M12a | Liquidation: one transfer in, and where it went | D12 | DONE | 2026-09-11 | `/accounting/liquidation` — per transfer, not per month (D106). Cashflow dropped: the ledger already is it (D108) |
| M12b | Payment calendar: 12 months, projected against actual, with due dates | D12 | DONE | 2026-09-11 | `/accounting/calendar` — one estimate per line with the day it is due (D109), *not in the plan* reconciles it to the ledger (D111), and the verdict names the month the money runs out |
| M12c | Calendar: weekly / monthly / one-off, and a month opened day by day | D12 | DONE | 2026-09-11 | three shapes per line, `amount` per occurrence (D113); a month expands into its dated movements with the first day it goes under and the lowest point (D115). Counting paydays moved the failure from December to **November** |
| M13 | Polish: phone, empty states, refusals, demo reset, guided tour | D13 | DONE | 2026-09-11 | every screen checked at 390px (no horizontal scroll anywhere, drawers full-screen with a pinned action bar); every table carries an empty state that says what to do; the guided walk is `?tour=flow-b` (D117); the dashboard now reads the demo store instead of the shell's sample data (D118) |
| M56 | One business, two schedules — and three more answers from the owner | — | DONE | 2026-09-13 | **Q44 (D270)**, open since F70 and the sharpest of them, because a single start time made it **arithmetically impossible for anybody in the building to be late**: the workshop taps in at 06.49 against an 08.00 rule with fifteen minutes of grace on top, while one man carried a hand-typed Rp 45.000 lateness deduction. Production starts 07.30, the office 08.00, the break is 45 minutes. The start time is a **map per unit inside the dated rule book** rather than a column on the employee, because it is a rule about a unit and changing it next March must not rewrite what last March was measured against. The new version is dated to the day v3 began, **not to today**: the workshop always started at 07.30, so 08.00 for them was never policy but an unasked question, and dating the correction from today would make the system assert something false about September — safe to backdate only because `late_mode` is manual and not one rupiah has ever been computed from it. On this data it moves late morning taps from 17 to 60. **Q45 (D271)** — IT types the yearly figure, HRD and payroll read it, and the monthly average is derived from it rather than stored beside it; the access split was already what the screen did. **Q46 (D272)** — a half day earns the full tunjangan, now a ruling rather than our reading. **Q48 (D273)** replaced its own question: the owner said to ignore re-sanding and pointed at something bigger — several vendors each doing one process, and a piece that can visit more than one. Not a third route; raised as **W6**. Two findings. A KPI window read **every day against the book in force on its first day**, so a range spanning the correction reported a workshop that was never late — F70's illusion arriving a second time through a different door, and F68's shape again: one value doing two jobs (F89). And the break allowance, written into `issues`, sent a perfectly readable day to review and **stopped it being paid** — `notes` now sits beside `issues` because *the rule could not fit the taps* and *this is worth a look* are different sentences (F90). The 18-day swing that exposed it was chased to the baseline and turned out to be a mid-edit artifact, which is why it was worth measuring rather than accepting |
| M55 | Two roads to a confirmed order, evidence that opens, and a history with an honest edge | — | DONE | 2026-09-13 | the last three backlog items, and each one turned out to be about the same thing: **a screen saying something it could not back up.** **W2 (D267)** — an order is a promise made in the company's name, so leadership confirms it before it is sent (D132), and until now that was one road for everybody including the person who *is* leadership: draft, ask, receive a chat card addressed to yourself, approve. Three acts for one decision, and theatre in an approval trail is worse than nothing because a rubber stamp looks like a second pair of eyes. Now an author who already holds `approve_goods` confirms in the same act, `self_confirmed` is **stored** rather than guessed from `created_by === approved_by`, and both the toast and the banner say plainly that nobody else looked. The second road is the one W2 called missing — *the card, not the plumbing* — and it is the road a request batch already takes (D69): a token goes out, the answer comes back from the approver's **own** account, and Putri answering Evin's card is refused with the reason. **B2 (D268)** — three chips reading *photo of the goods*, *tanda terima*, *on file* were labels **about** evidence rather than ways **to** it. The fix was in the contract: booleans became attachment ids, and presence is derived from the id where it is rendered, so the chip and the link cannot disagree. **B4 (D269)** — paging made the decided history usable; it never answered *how far back is worth showing*. Ninety days by default, and the card **always says what is outside it** and names the oldest date, because a list that quietly stops somewhere lies by omission. Two findings, both from driving the screens rather than reading the diff: a toast still telling leadership to go and ask for an approval the API had just granted them (F87), and — within a minute of the chip becoming openable — a file called `tanda-terima-hadi-0708.jpg` sitting behind *photo of the goods* on two seeded receipts (F88). **A label nobody could open is a label nobody could check.** Refusal table now 25/25 |
| M54 | The BOM proposes, the storeman disposes — and the rack finally has a list to be measured against | — | DONE | 2026-09-13 | the half of S1 (Q40) open since M27: nothing connected a bill of material to what actually left the rack, so an issue was recorded item by item against no expectation at all. The obvious build is automatic deduction — report progress, subtract the BOM — and it is the wrong one (D266). **Stock that moves because somebody typed a progress entry is stock nobody counted**, and the rack then disagrees with the screen in a way only a stock-take can find, months later, with no way to tell which of the two was wrong. So the BOM is a **proposal**: the panel pre-fills what the run still needs and the storeman edits it to what he actually carried out, because he is the one who carried it. Expectation comes from the order's **own pinned revision** (D256), never from today's catalogue. A variance is **not readable until the run is finished** — half a run has taken half its material, and calling that a 50% saving teaches people to ignore the figure — so the panel prints *3/4 jadi* instead of a percentage. An item issued that the BOM never named is **di luar BOM with an em dash**, not a variance of zero: the list forgetting the glue is a different fact from the glue being unexpected, and the more interesting one. Over-issuing stays allowed and flagged (A6), now said **before** the confirm as well as after, because a storeman about to send the count negative should find out while he can still change the number. One finding, and it is F84's twin two commits later: nine seeded issues and one return pointed at **two work orders that have never existed**, invisible for six milestones because nothing in the system had ever joined a stock move to a work order (F86). A key nothing follows is a key nothing checks. The seed is repointed and the quantities deliberately left alone — 85 sheets of amplas against a list calling for 32 is what this business will actually see on its first day with a BOM behind the rack. Refusal table now 23/23 |
| M53 | A name on the work becomes a person, without the software ever deciding which | — | DONE | 2026-09-13 | W5, and the limitation every KPI card had been apologising for since M51. Production records a **name** because a subcontractor is a legitimate answer to *who did it*; the fix is a link **beside** that name, never instead of it, with `worked_by` kept verbatim for ever (D264). Three states and genuinely three: linked, **confirmed not one person** (*Tim potong*, a vendor's crew), and **nobody has been asked yet** — the third is not a worse second, it is the only one that is somebody's to resolve. The pair is never read raw; everything downstream reads a derived `attribution`, and the API refuses a name declared both at once. `/produksi/penautan` asks **once per name**, not once per entry, because a screen that asks six times is one somebody abandons halfway — and a half-resolved record is worse than an untouched one, because it looks complete. The suggestion is a suggestion: offered, never applied. **Coverage is a property of the record, not of the person** — nobody can tell whether an unresolved entry belongs to a given person, so every empty production column prints the period's coverage beside it, and on the seed three answers move it from 29% to 71%. The work is **shown and not scored**, for a new reason now the old one is fixed: **a piece is not a unit** — eight nakas and four wardrobes do not add up, so it sits beside the scores as the *deliverable* half of what the owner asked for, the same shape as overtime under D261. The owner's *lewati menyentuh skema lama* closed two open items in the same commit (D265): every Phase 2 schema is renamed `ops_*`, because the live project already has a `core` schema and `create schema if not exists core` would have quietly put this system's tables inside the old one — with `set search_path = core` on the security-definer functions as the sharper edge, since `has_permission()` would have been reading somebody else's tables while looking correct. `0001` now refuses rather than merges. `core.audit_exempt`'s disabled RLS stays reported and untouched: it is the old system's. Two findings, both found by reading rather than running: two seeded rows sharing a primary key, invisible for six milestones because nothing had ever dereferenced one (F84), and an ambiguity guard that was blind in the one case it existed for, because equality made it certain about the most collidable name in the register (F85). Refusal table now 21/21 |
| M52 | Peti with a code on the outside, and the QR that opens it | — | DONE | 2026-09-13 | #12, and the finding came before the code: **half of it was never waiting on a backend** (F82). It had sat in the backlog as one item because both halves are "QR" — which is a fact about the technology, not about the problem. The question that separates them is **who is holding the label**: a vendor has no account here, so their PO QR needs a public route and a token and stays in Phase 2; the person scanning a packing box is our own installer, already signed in on the phone in their hand. A delivery note says *2 set meja makan* and a lorry arrives with nine crates; the box record carries the one fact the note structurally cannot, **which room this crate goes to inside the building** (D262). That is compulsory at the API and the largest type on the label, because a label without it just moves the job of opening the crate to the site. One refusal — a box cannot be marked fitted before anybody has scanned it, the installation rule one level down — and everything else **warns**, because a box scanned on site that the record says never left is in somebody's hands and refusing the scan would mean it is never recorded at all. The only other compulsory field in the flow is the sentence explaining a problem. `position` (*3 dari 5*) is derived, never stored. A June consignment from before the labels has **no** boxes, said as *tidak ada peti tercatat*, never *0 peti*. Then the QR's own content was decided twice: the code-only design was argued well and was wrong, because it never asked what the scanner is — a stock phone camera, which turns a bare code into a line of text to retype, and an in-app scanner would need a WASM decoder since `BarcodeDetector` is absent on iOS Safari (F83, D263). It holds a URL, the code is printed under it in mono so a moved domain degrades to typing, and the millimetres were **measured**: 0.86 mm per module against the ~0.5 mm a phone needs. Labels are 97 × 88 mm, six to an A4 sheet, in real millimetres on screen as well as in print. QR geometry is verified end to end by decoding the emitted SVG path back with `jsqr`. Refusal table now 19/19 |
| M51 | A task tracker, and a KPI that refuses to score what it cannot measure | — | DONE | 2026-09-13 | #11, and the first module in this system that measures **people** rather than money or goods — so every rule in it exists because its opposite would quietly hurt somebody (D260, D261). The tracker comes first, because a score over deliverables with no record of what was asked of anybody is a score over opinions. **A blocked task leaves the arithmetic entirely**: a tracker that punishes people for reporting blockers stops being told about them, so blocking requires a reason at the API. **Unmeasured is not zero**, **a thin basis is not a measure**, **entitlements are not absences**, and a combined score does not appear below a minimum number of measures, because a score over one axis is that axis wearing a costume. Overtime is shown and deliberately not scored. Every measure prints its **source** and **what it was computed over**, so the figure can be argued with. Production work does **not** feed the score and every card says so: it is recorded against a name, not a person, and matching people by string into a performance record is how the wrong review lands on the wrong person. One finding, three parts, and the first part is F72 recurring two days after it was written down (F81) — a rule in a comment does not protect the code under it. The seed could not demonstrate the module at an honest floor, and the right answer was not to lower the floor: **a calendar month is the wrong period for performance**, so the screen takes a date range and defaults to the last payroll run's. Refusal table now 16/16 |
| M50 | The enrolment register, and an invoice audited against the roll of names | — | DONE | 2026-09-13 | #10b, and the half of Q30 that was left open in M45. The owner replaced the question — *which deductions and at what rate* — with the one that matters: **the rates are published and the roll of names is not** (D259). So HRD keeps the register, accounting audits the bill against it, and the arithmetic is the simplest thing a person can check by hand. It catches the leak he described on the seeded data: BPJS Kesehatan paid Rp 1.525.000 for three people who should cost Rp 1.225.000, **because Made Suparta came off the roll on 31 August and the invoice did not notice**. Ending an enrolment keeps the row, the date and a required reason — that sentence is what accounting reads when the name is still on next month's bill. The payslip shows the employee half **only for people HRD actually registered**, so nobody is short a rupiah because software was updated, and the employer-only schemes are named as cover that costs nothing rather than printed as a deduction of zero. **PPh 21 is recorded and never computed** (D140 unchanged). One finding: auditing per scheme produced four red rows describing one healthy payment, because one BPJS TK invoice pays four schemes and the money moves per invoice, not per scheme (F80). JKK's rate is the one figure that is **not** published — it follows the employer's risk class — so it is seeded as a stand-in, flagged unconfirmed on screen, and asked as Q49. Refusal table now 14/14 |
| M49 | A layered bill of material, a request that contains the whole wardrobe, and a labour cost nobody invented | — | DONE | 2026-09-13 | #9. *What do I have to buy for this run* becomes a **walk** rather than a sum (D257): waste compounds down the tree, a material reached by two routes is one line with both routes named, and a sub-assembly with no released BOM stays in the list as itself rather than vanishing. Loops are refused where they are written — anywhere in the chain, not just self-reference — with the message naming where the loop closes, and still detected on read. Labour is **typed, with the working beside it** (D239): null until somebody computes it, never derived from the pay rules or from hours, because it flows straight into a quoted price; the combined total is null while either half is. Building it uncovered a gap that had been open since M23: *Buat PR dari BOM* filtered to `kind === "material"` and so raised requests for six wardrobes containing **none** of the plywood, runners or screws their twelve drawer boxes are made of — while the summary above it read correctly, because the total costed the sub-assembly and the list did not (F78). Then the demo's own refusal table began failing one run in four, and the cause was the check racing itself: StrictMode invokes the mount effect twice and the probes mutate one global acting user (F79, D258). Refusal table now 12/12, four runs in a row, byte-identical |
| M48 | A bill of material that is versioned, and a work order that remembers which one it used | — | DONE | 2026-09-13 | #8, and the owner reversed the default to get it (D256). A draft is edited, a released revision is frozen for ever, and a work order pins the revision it was written against — so its projection is measured against the list it was actually written from, not against whatever the catalogue says today. Editing a released line is **refused** rather than quietly redirected into the draft; opening a draft **copies** the released revision, because a draft pointing back at released lines would edit them by the back door. Releasing refuses an empty revision, an unchanged one, and one with no sentence behind it. `bom_rev: null` means the order predates versioning — never "the current one" (F60's rule, still earning its keep). Where the catalogue has moved on the order says so and keeps its own revision; moving it is a decision with a reason, refused once anything has been built. Two findings, both about **two things that had to agree** — a sub-assembly priced off every revision at once (F76), and a diff fetched beside the list it describes instead of derived from it, which confidently listed four additions that had not happened and omitted the one that had (F77). Two more rows in the demo's refusal table: 10/10 |
| M47 | Four production stages, and a route for work the vendor builds | — | DONE | 2026-09-13 | #7. Seven stages become **Pembuatan · Finishing · QC · Packing** (D253), and the owner's reason for simplifying is what set the number: a subcontracted piece could only be described by seven stages as *four of them mysteriously skipped*. So a route is a **list of stages** — `IN_HOUSE` has four, `SUBCON` has three — and a subcontracted order has no *Pembuatan at 0%*, because an empty bar would say nobody has started building goods the vendor has already built (D254). Where the goods are is three dates and no status field; when the vendor's promised date passes the badge says **vendor telat**, not the workshop's own *terlambat*. Reporting work on goods that are not in the building is **refused** — the one place production stops warning and starts refusing, because every other refusal here is about a number that cannot be true and this one is about a place (D255). Progress recorded under the seven keeps its codes and rolls up on read, which is where the change nearly went wrong: `FINISHING` names one of the four *and* one of the seven, so "direct" and "rolled up" entries were added together and the board printed **Finishing 7 of 4** (F74). Building a work order through the interface then found the same rule written twice and already drifted (F75). Two new rows in the demo's refusal table, both passing |
| M46 | Pay splits into pokok + tunjangan, and an hour stops being 1/173 of a month | — | DONE | 2026-09-13 | #6, the largest of the deferred answers. Everybody's pay is now **pokok + tunjangan**, the tunjangan earned per day present and taken away only by HRD with a reason — its own append-only record, not a flag on the day mark, because WFH is a worked day with a right timesheet and no allowance (D250). An hour of somebody's time is **setahun gaji ÷ hari kerja efektif ÷ jam sehari**, the owner's own arithmetic; 173 stays where the law puts it and both figures print side by side on the rule screen, about 10% apart on this office's numbers (D249). Lateness gets the threshold the owner set — 15 minutes, priced by the hour — computed always and applied never, until somebody switches it on having seen it priced per person (D251). Four findings, and the pay split caused three of them: a field called `late_after_minutes` that held a start time and left the owner's grace period nowhere to live (F70), a worked example that had quietly stopped running the rules and was right only by coincidence (F71), five office salaries cut by Rp 600.000 because no taps was read as absence (F72), and bruto and diterima computed by two longhand sums, one of which was updated (F73) |
| M45 | The interview answered: nineteen questions closed, five of them built | — | DONE | 2026-09-13 | the open-questions table is empty for the first time since D14. Nineteen answers recorded as D227–D249, and the five that were small enough to build shipped in the same commit: the Rp 2 juta no-approval limit as a **setting** the liquidation drawer flags against (D231); boards with no costed load valued at the **dearest** load of their species, marked `±` at every level rather than averaged (D232); the cash estimates moved to leadership alone via a new admin-only `plan_cash`, guarded at the API and not merely hidden (D233); a payment term's due date becoming the **expected delivery date** until something actually arrives, which closed a real gap — the largest payments in the system had no date anybody could plan around (D234); and Q39 closed with **no selector added**, because logs are cut the same day and a choice in the interface would misdescribe the yard (D235). Also `/accounting/tagihan`, the monthly bills list, as a second shape of the cash calendar and never a second calculation (D228, D229). Three findings came out of the build, all of the same family: a comparison column that could not work and then compared a week against a month (F68), the same bill appearing in two of three lists that should partition the month (F69), and a payment term with no date anybody could plan around (F67). F68's fourth act is the one to remember — `cashPlan(state, now)` had used one argument for *where the window starts* and *what counts as today*, which agreed perfectly until something asked for a past month, and August then opened with four overdue paydays reading *belum jatuh tempo*. Fixing D234 also found `poTerms` and `poDetail` still reading the UTC date after F63 had consolidated the office day |
| M44 | Responsive fixes, and two languages with an honest boundary | — | DONE | 2026-09-13 | measured every screen at 390 / 768 / 1280: no overflow anywhere, and three real faults the overflow test could not see (F65) — the floating launcher sitting permanently on the last row of every list, badges breaking mid-phrase inside their pills, and the handover board's five comparison columns **losing the screen's entire argument** on a phone. Then language: the shell and all of John Lau are bilingual, screen bodies are not, and the business's vocabulary never is (D224) — said on the setting row rather than implied. Switching to English first made John Lau fail his own English example prompts (F66), so the router now understands both regardless of the interface language (D225). Pay and attendance confirmed closed (D226) |
| M43 | John Lau: a prompt that runs named tools, and cannot reach past them | — | DONE | 2026-09-13 | the old system comes back as the assistant. It turns a sentence into **named calls against the same API the screens use** and shows what they returned, with the tool's name and a link to the screen holding the same number — it never composes a figure (D217). Berkas 201 and all of IT are closed at every grant level, and the closed capabilities are **listed rather than absent** (D218); the catalogue is a data file the dispatcher reads, not a system prompt. It acts with the grants of the person typing and nothing else (D219). Writes are drafts confirmed field by field (D220). The panel is in the shell, so the SOP stays open while you walk to the screen it describes (D223). The keyword matcher is the swappable half and the screen says so (D221) — building it found a false refusal accusing somebody of reading staff records when they asked about late work orders (F64) |
| M42 | Pengaturan: the settings sorted by what they rewrite | — | DONE | 2026-09-13 | the last placeholder. Twelve settings, each labelled by **what it does to figures that already exist** (D214) — seven safe to change here, five that would re-read the past and are refused **at the API** with the reason said out loud (D215), two of those linking to where they really change. Every editable one is wired to live code (D216); the brand name reaches the sidebar and the vendor's PO, the number format reaches every rupiah. Building it found the office-day offset copied into **thirteen files** (F63) |
| M41 | Accounting's payslip screen removed | — | DONE | 2026-09-13 | owner: accounting does not read payslips. An empty route deleted rather than parked — D105 parked two *working* screens over a judgement that might change; a placeholder kept after its question is answered tells every future reader the module is unfinished (D213). Payroll stays HRD's, in one place. `/accounting/budget`, which the frontend plan still listed as a placeholder, never existed — the cashflow calendar answered it |
| M40 | The last leg: delivery, installation, handover | — | DONE | 2026-09-13 | the three remaining placeholders, built as a tenth service (D209): a truck, a site, a crew, a snag list and a signature are neither buying nor making. The board answers what the order board cannot — **ordered, made, left the yard, arrived, installed** per line, and the gaps between them. Shipping what is not made and fitting what has not arrived both refuse (D210); a handover refuses without the signed BAST (D211) and freezes the open snags onto itself so it can never read as a clean signature (D212). Building it found *no work order at all* being counted as **made zero** (F60), goods on a truck counted as **on site** (F62), and that **nobody in the seed could open these screens at all** (F61) |
| M39 | Purchase verification: the picture, and the web around it | — | DONE | 2026-09-12 | owner. A preview on every document, swapping in place as you click between them — B3 closed. Then the part that is not cosmetic: the three cardinalities the owner named (one document → several transactions, one transfer proof → several purchases, one purchase paid twice in cash and transfer) were all **already possible in the data and invisible on the screen** (D206), which is how a nota gets booked twice. The check sits where it bites — on the row being linked to (D207). The preview says *contoh — bukan pindaian* across its own face, not in a caption a screenshot would crop (D208). Seeding the split payment caught the bank row still carrying the full amount, and a CONFIRMED inbox row whose document was attached to nothing (F59) |
| M38 | Kayu: the nota that must be recognised before it is read | — | DONE | 2026-09-12 | owner, three asks and one purpose — kubikasi vs harga, stock, usage. A load is entered **from its nota** (D201), and a nota kayu is **recognised before it is parsed** so its thirty size rows become boards rather than thirty ledger lines (D200); the recogniser shows its reasons in words and writes nothing until somebody agrees. Logs and boards merged into one module (D202). **Q40 answered**: boards are a movement ledger, with the sawn side derived from the sawing reports so the rack cannot disagree with the rendemen (D203). An issue from an unknown stack keeps its quantity and loses its value rather than guessing (D204). Building the reader caught it reading the nota's own date as a 20-metre plank — and counting that plank as evidence the nota was timber (F58) |
| M37 | Berkas 201: the number you have to ask to see | — | DONE | 2026-09-12 | owner: documents go to Drive and the number is read out of them; identity numbers show masked and HRD clicks an eye to read one, which lands in the IT audit. Masked **completely**, not by prefix — a NIK's first six digits are province, city and district, and nothing here needs digits to tell two rows apart (D195). The mask is in the payload, not the markup, or the log would record a click while the number sat in the browser (D196). A reveal is an audit act, counted apart from changes, and the row never contains the number (D197). The misaligned column was a missing caption, not a width. Building it caught the seeds claiming seventeen numbers were *read from the file* with no file attached (F57) |
| M36 | Pay rules: HRD reads them, IT changes them | — | DONE | 2026-09-12 | owner: *aturan gaji HRD hanya lihat, IT yang bisa menggantinya* (D193). The form still renders in full for HRD — every ladder, every threshold, the worked example — and every control is disabled, with the reason said plainly rather than a screen that is simply missing. The change needs `it: write`, so the people whose payslips the rules compute are not the people who can change them alone. Verified by calling the guarded endpoints directly as each person, not by reading the screen: HRD is refused at the module, leadership at the level, IT passes |
| M35 | Who may read the IT module — and the guard that could not tell | — | DONE | 2026-09-12 | owner closed the other half of Q22: *yang boleh baca module IT hanya IT dan pimpinan* (D190). Leadership holds `it: read`; administering stays at `admin`. Building it found the real hole: `requireModule` never looked at the level, so a read grant could have purged the activity log, and `setModules` / `setAuthorities` had **no gate at all** — anybody acting could have granted themselves anything. `requireLevel` fixes both (D191). `Aturan penggajian` moved under Payroll, where its permission always pointed, so the IT heading stops claiming something untrue (D192) |
| M34 | IT: the audit trail, the activity log, and who holds what | — | DONE | 2026-09-12 | asked directly whether IT was complete; it was not — four of its five screens were placeholders. Audit log, activity log, users and roles are now real. Q22 answered the retention half (D188): detail for 30 days, daily recap for six months, and the recap is stored because it must outlive its source. Purging is the only deletion in the system and it **refuses a day that has no recap yet** (D189). The audit screen shows a refused approval beside the approved ones, because a trail that only records what succeeded is missing the half people argue about |
| M33 | Package, worldwide: markets instead of a flat area column | — | DONE | 2026-09-12 | owner: the SP NORTH / SP MIDDLE split only reads as a location because we are in one country. Geography becomes country → region → city → local label, with the market carrying its **currency**, **time zone** and language (D187). One filter at three altitudes — countries, then that country's cities, then its districts — because the scope is a prefix of the market code. The seeds now hold Sydney and Bali at day one alongside the live Gold Coast, so an ADR in AUD never lands in the same figure as one in IDR |
| M32 | Package: the agent pipeline, and the commission that follows | — | DONE | 2026-09-12 | owner's own tracker, rebuilt as the ninth service (D183). Three agents per property approached in order, with the **seven-day move-on rule derived** rather than maintained — the demo has an agent nine days silent the sheet would still call *MSG SENT*. The funnel counts properties by their furthest agent. An agent who agrees is onboarded with a rate, and a DEAL without one is refused (D185); commission is computed only from contracts that exist and is paid through the ledger, not here (D186). Marketing's three placeholder screens are gone |
| M31 | Rekening koran: how the leadership accounts reach the ledger | — | DONE | 2026-09-11 | owner: the statement is uploaded because 064 and USD 081 are held by leadership and not shared. So importing one **creates** ledger rows rather than ticking them off (D180), a dollar line waits for a typed rate (D181), and the file has to balance against the two figures printed on its header before anything is booked (D182). The demo file catches the two movements nobody in the office could see: a bank charge and a payment made straight to a timber vendor |
| M30 | Desain: the drafters' queue, and the revision the floor is cutting from | — | DONE | 2026-09-11 | owner: the module must make the drafting team's work easier (D179). Blocked first — a question waiting ten days on a client; then the dangerous case, a revision uploaded and never released while the workshop still cuts from the older one; then what is undrawn, computed from the orders themselves. Uploading and releasing are two acts, and a release is **refused** while a question is open. Building it also caught a finished June project making a released drawing read *lewat 74 hari* (F51) |
| M29 | Berkas 201, and cuti asked for before it is marked | — | DONE | 2026-09-11 | owner: *cukup di berkas 201 dan cuti/izin, compliance skip*. The personnel file becomes a checklist that names what is missing and warns what expires — a forklift licence that lapsed in July and two PKWT contracts inside sixty days, none of which anything could see before (D177). Leave gains the half that happens first: asked, decided, and the approval is what writes the timesheet marks, skipping days that already carry one. Over-entitlement is shown before the decision, never refused (D178). Compliance is dropped (D165) |
| M28 | Pay rules: the policy, as dated data | — | DONE | 2026-09-11 | owner: *skema gaji dan aturan harus customizable*. Four situations — upah harian, upah per jam, lembur, undertime — each a number somebody can change under IT rather than a line of code (D173). Overtime follows the national ladder (1,5× / 2×, rest day 2×/3×/4×, monthly ÷ 173), which **answers Q31**; the form's own GAJI still wins where it speaks. Changes write a new dated version, are refused into the past or inside an existing run, and cannot be saved until the preview shows what they do to each person (D175). Undertime ships **off** (D174) |
| M27 | Stock: bahan & hardware, counted from their movements | — | DONE | 2026-09-11 | owner: *bangun inventory/stock, kategorikan tiap item dengan baik*. The catalogue is re-filed two levels deep and **counting is a property of the category** (D169); on-hand is the sum of the moves, never stored (D170); confirming a delivery puts the goods on the rack, which closes the oldest gap in the system. An opname stores the **difference** with a reason and a location (D171), and stock that arrived unpriced is counted but kept out of the valuation and named (D172). Open: what an issue costs — FIFO, average or standard (Q43) |
| M26 | Pagination everywhere, and a payroll week you can slide | — | DONE | 2026-09-11 | owner, twice over. Every table pages at 25 rows with the count printed, and the lists that are not tables — the attendance grid, lembur sheets, timber loads, the verification queue and its history — page the same way (D157). `/hrd/payroll/minggu` walks Senin→Minggu with ‹ ›: a week with no run still has figures, because nothing is stored (D158) |
| M25 | The company's own form lembur, and a payslip worth printing | — | DONE | 2026-09-11 | owner: *kita upload, sistem baca*. The real *FORM LEMBUR PT TALAHOME* is imported against a sheet — names matched, unknowns listed back and never created, **GAJI paid as written** (D154). The payslip becomes a card: as many per A4 as measurably fit, with the owner's own weekly recap (masuk · pulang · jam · lembur) and every hand-typed potongan printed with its sentence (D155, D156). Where the grid and the total disagree — a day nobody has read, overtime nobody approved — the slip says why rather than leaving it to be discovered (F48) |
| M24 | Timber: log → papan → kubikasi → harga per m³, per vendor | — | DONE | 2026-09-11 | the eighth service (D153). Logs measured stick by stick, boards measured and counted, and the number that decides a vendor is **rupiah per cubic metre of board**: Kayu Manis is Rp 15,5 juta per log metre against Sumber Kayu's Rp 18,2 juta, and Rp 34,6 juta against Rp 29,6 juta once sawn. Yield only over the logs actually cut; vendors compared per species. Also: any PR line may name the job it is for (D152) |
| M23 | BOM → PR, and biaya produksi: proyeksi vs aktual | — | DONE | 2026-09-11 | a bill of material becomes a draft purchase request whose lines carry the **SPK number** — which is the one column that makes the end-of-project question answerable: did this cost more or less than we thought. Materials against materials; the ledger's wider project total is shown apart rather than subtracted (D151) |
| M22 | Gambar kerja, gambar jadi, ukuran — and the items on an order | — | DONE | 2026-09-11 | owner: every item must carry a working drawing, a finished picture and a size. Taken literally that means the system has to be able to say which are **missing**, so size is three numbers rather than a sentence and the catalogue names the gaps. Orders gain lines — item and quantity — read beside *dibuat* and *selesai* from the floor (D150) |
| M21 | Master data: projects, products, bills of material | — | DONE | 2026-09-11 | a project gains the fields that make it an order rather than a tag — client, PIC, dates, contract value — with the **code fixed** because every service references it as text. Products are their own table, not `procure.items`: those are bought, these are made (D149). A BOM carries susut apart from quantity and prices itself from the catalogue on read, marking what it cannot price instead of counting it as zero. Open: BOM versioning (Q36), where a quotation lives (Q37), labour cost (Q38) |
| M20 | Two kinds of lembur sheet, and the production board they feed | — | DONE | 2026-09-11 | only **production** overtime needs leadership — one sheet, many names, item · proses · berapa against each (D146). A staff session is one report and HRD's decision, paid by default. Signing a production sheet **posts the work to the production board** (D147), which is a seventh service: work orders with seven stages, deadlines, and late-first ordering (D148). Open: are these the right stages (Q35) |
| M19 | What a marked day is worth, and two signatures on every overtime hour | — | DONE | 2026-09-11 | answering the owner on Q33 and Q34. *Sakit* is paid with the surat dokter attached, *cuti* out of a per-person balance counted from the marks, everything else recorded and unpaid (D144). Overtime needs HRD **and** leadership, and leadership's approval is refused until the surat lembur is linked — a fifth authority, `approve_overtime` (D145) |
| M18 | Timesheet: the reader's own file, the six taps of a day, and what HRD says about it | — | DONE | 2026-09-11 | built against the owner's real export — 981 taps, 35 people, **48 of 227 days the rule cannot read** (F40). Attendance is stored as taps, not days (D141); HRD marks a day for what it was and marking never touches a tap (D142); an import reports an unknown machine number and creates nobody (D143). Open: which marked days are paid (Q33), who may approve overtime (Q34) |
| M17 | HRD: employees, biometric attendance, overtime, payroll and payslips | — | DONE | 2026-09-11 | `hr` is a sixth service (D136). A day with one stamp is worth nothing (D137), overtime is claimed and approved rather than inferred (D138), a run cannot be approved over open days (D139), and payroll computes **gross** and says so — deductions are Q30–Q32 |
| M16 | Receiving as report + confirmation; PO confirmation, PDF to the vendor, expected delivery | — | DONE | 2026-09-11 | goods arrive at night, so a report needs only a photo and only a confirmation counts (D131). An order is confirmed by leadership before it is sent (D132), leaves as the app's own print view over WhatsApp (D133), and carries the date the vendor promised (D134) |
| M15 | Purchase orders: the module, with terms, amendment and closing | — | DONE | 2026-09-11 | `/procurement/po` was still a placeholder. Now the order-first half of the tracker's question, with the payment schedule and its guard (D128), amendment by supersession (D129) and a close that refuses politely (D130) |
| M14 | **Walkthrough + findings → the schema we actually need** | D14 | DONE | 2026-09-11 | `checkpoints/2026-09-23.md` is the walk; `02-database.md` gains **What the walk changed** — fourteen schema changes a working screen forced, none of them visible on D1. Phase 2 proposed below with dates |

**Deployment.** Vercel builds from GitHub directly, not from a developer's
machine, so nothing in this repo has to hold a Vercel credential.

**Vercel's Production Branch is `claude/serene-euler-eq2qef`** (owner, 2026-09-11).
Phase 1's whole output lives on one branch, so pointing production at it beats
merging a half-finished phase into `main` to see it. Every push here now
deploys to production, and the stable project URL always shows the latest work.

Consequences, so nothing is a surprise later:

- `main` deploys nowhere until this is changed back. It still holds the
  original shell, and Phase 1 never merges into it — Phase 2 decides what
  `main` should become.
- Production deployments are public; Deployment Protection only gates previews
  by default. The link can be shared with the team as-is.
- Reversible in one setting, whenever `main` should take over again.

### Phase 2 — the backend (kit written 2026-09-11 · `docs/plan/phase-2/`)

The D14 proposal (P1–P8, dated to late September) is **superseded**: it was
written before HR, production, inventory and master data existed, and its
ordering by layer has been replaced by ordering per schema. The brief the
build session works from is `docs/plan/phase-2/` — readiness, the ladder, the
API inventory and the estimate.

| # | Milestone | Sessions | Status | Note |
|---|---|---|---|---|
| B0 | Foundation: schemas, enums, identity, audit, numbering, evidence | 0,5 | **DONE** | `supabase/migrations/0001–0006`, applied from nothing against Postgres 16. `rebuild.sh` re-applies the ladder; `smoke.sql` proves the access model **refuses** — HRD cannot approve funds, `write` is not `admin`, an authority is never implied by a level |
| B1 | The remaining 21 migrations | 3–5 | TODO | procure PR/rounds/PO/receipts · acct ledger/inbox/calendar · hr · prod · inv |
| B2 | Derivations → views | 4–6 | TODO | 2.583 lines of TypeScript. The demo's figures are the test data: if `v_cash_plan` does not also say *November*, one of the two is wrong |
| B3 | Write seams → RPCs | 3–4 | TODO | ~25 functions, each with audit + outbox + refusal + idempotency |
| B4 | `src/lib/api` and the swap, one service at a time | 2–3 | TODO | same signatures, so no screen changes |
| B5 | Supabase Auth and real sessions | 1 | TODO | `actAs` goes; the persona picker becomes dev-only |
| B6 | Storage: bucket, signed URLs, sha256 | 1 | TODO | the evidence road end to end |
| B7 | Smoke + RLS matrix tests | 2 | TODO | one refusal and one derivation per schema |
| B8 | Data migration from Sheets + `john-lau` | 2–8 | BLOCKED | the only open-ended item: nobody has yet written down what must come across and in what state |
| B9 | Parallel run and cutover | 2–3 | TODO | both systems in use, figures reconciled daily |

**The ordering rule.** Per **schema**, not per layer: finish `procure` from
table to view to swapped screen before starting `acct`. A vertical slice
proves the pattern; four horizontal layers prove nothing until the last one
lands. Identity comes first regardless — every refusal elsewhere is theatre
until the database knows who is asking.

---

## The documents

| File | What it settles | Phase |
|---|---|---|
| `00-context.md` | What we carry over from `john-lau` — the rules that bind, and the eleven things we deliberately do differently | both |
| `01-architecture.md` | Service boundaries, ADRs, what runs where | both |
| `02-database.md` | **Target** schema and ERD. Not built in Phase 1 — it is the shape the demo types are cut to | 2 |
| `03-api.md` | **Target** REST surface. The demo layer implements this contract exactly | both |
| `04-frontend.md` | Design system rules, screen inventory, interaction contract | 1 |
| `05-workplan.md` | Day by day D1–D14, with the exact prompt for each day | 1 |
| `06-decisions.md` | Decision log + open questions, each with a default so nothing blocks | both |
| `07-ways-of-working.md` | Phone-driven workflow, session protocol, prompt recipes | both |
| `backlog.md` | What somebody reported while using it, bugs and asks alike — kept out of the milestone board so neither list lies about the other | 1 |
| `findings.md` | **Written as we go.** What each screen taught us about the rules — the input to Phase 2 | 1 |
| `phase-2/` | The build kit: readiness verdict, the migration ladder, the API inventory, the estimate, and the protocol that lets the design session keep running while the backend is built | 2 |

---

## Ringkasan (ID)

Kita membangun ulang sistem operasi internal Tala Living. **Tahap 1: seluruh
alur kerja dibangun di frontend dulu, memakai data contoh, langsung
di-deploy ke Vercel** — tanpa basis data, tanpa backend. Semua tombol
berfungsi, semua perpindahan status terjadi, tetapi datanya hidup di
peramban.

Alasannya: aturan bisnis memang belum pernah ditulis di awal. Aturan seperti
itu tidak bisa dikarang dari skema — ia ditemukan dengan **menjalani** alur
kerjanya dan melihat di mana layarnya tidak bisa menjawab pertanyaan. Selama
Tahap 1 skema masih bebas berubah; mengubah data contoh cukup menyunting satu
berkas TypeScript.

Tahap 2 (setelah Tahap 1 dijalani dan disetujui): backend sungguhan di
Supabase, memakai rancangan yang sudah ditulis di `02-database.md` dan
`03-api.md`. Tahap 1 dibangun **mengikuti kontrak itu**, jadi menggantinya
berarti mengganti satu modul, bukan menulis ulang layar.

Gaya, komponen, dan interaksi frontend memakai yang sudah ada di repo ini —
tidak didesain ulang. Aturan uang dari `john-lau` dibawa utuh (persetujuan ≠
pembayaran; uang ≠ barang; append-only; peringatan bukan blokir; penolakan
harus terlihat).

---

## House rules for every session

1. Read this file. Do the named milestone. Update the board. Commit.
2. One milestone = one commit or one PR. Reviewable from a phone.
3. **The demo layer implements `03-api.md`.** Same signatures, same envelope,
   same refusal codes. A screen must never reach around it.
4. Never invent a business rule silently. Take the default in
   `06-decisions.md`, write down that you took it, and carry on.
5. **Append what the screen taught you to `findings.md`.** That file is the
   deliverable of Phase 1, as much as the app is.
6. Derived state is derived — computed from the demo store on read, never
   stored as a field that can disagree with it.
7. A refusal is visible: explicit outcome code, a toast the user can read.
   Never a silent success.
