# 06 — Decision log and open questions

Two lists. **Decisions** are settled; reopening one needs a reason written
here. **Open questions** each carry a default, so nothing ever blocks — take
the default, note that you took it, and carry on. The owner can flip any of
them with a one-line answer from a phone.

Append, never rewrite. A silently dropped decision is how the same argument
gets had twice.

---

## Decisions

| # | Date | Decision | Why |
|---|---|---|---|
| 200 | 2026-09-12 | **A nota is recognised before it is read, and a timber nota is routed rather than parsed: its total is one figure for accounting, its rows are boards for inventory.** The recogniser shows its evidence in words — *31 baris berbentuk ukuran papan*, *menyebut Jati* — and writes nothing until a person agrees | owner: *umumnya transaksi dicatat dulu, dokumen referensi nota kayu harus bisa dideteksi sehingga tidak parsing semua papan ukuran menjadi transaksi*. Every other nota in this business is one row per thing bought. A nota kayu is thirty rows of **sizes out of one load**, so reading it the ordinary way puts thirty purchases in the ledger for one delivery of wood and doubles the spend report. A confidence percentage would have been cheaper to compute and impossible to argue with; a reason a person can check is the point |
| 201 | 2026-09-12 | **A load of logs is entered from its nota**, not typed | owner: *inventory log harus didapat dari upload nota*. The paper carries the price, the sizes and the date at once; a load typed beside it is a set of figures whose source is somebody's memory of the same paper. Loads recorded before this rule are kept and named rather than back-filled |
| 202 | 2026-09-12 | **Sawn boards are merged into the timber module** — one screen with three tabs (pembelian & kubikasi · stok papan · pemakaian), and `/inventory/papan` redirects rather than 404s | owner: *sawn boards sekalian gabungkan dengan logs saja*. They were two screens because they were two tables, and they are one module because they are one piece of wood either side of a saw. Split, the rendemen lived on one screen and the rack on another — which is how nobody noticed the rack was not stock at all |
| 203 | 2026-09-12 | **Q40 answered: boards are stock now, as a movement ledger — and the `sawn` side is derived from the sawing reports rather than copied into it.** What is on the rack is the sum of what came off the saw and everything since | owner: *daftar papan bisa menjadi inventory khusus log dan pencatatan pemakaiannya harus ada*. The old default said a figure that only ever goes up is not stock, and said so on the screen — correct until somebody wrote down what was taken. Deriving the incoming side means the rack cannot disagree with the rendemen, because they read the same rows. A stack is identified by **species and size**, which is how the workshop asks for wood; which load it came from is what it cost, not what it is |
| 204 | 2026-09-12 | **An issue names the load it came off when anybody knows, and leaves it blank when nobody does — quantity exact, value empty.** Never a load picked to make the arithmetic work | Q43 in the other material. The yard keeps stacks apart sometimes and tops them up other times, and the difference is knowable only to the person carrying the wood. A costing report with a made-up source is worse than one with a gap, because the gap is visible. The rack's own value scales the priced share to what is left, so an issue from an unknown load reduces the value proportionally instead of being charged to the nearest stack |
| 205 | 2026-09-12 | **The board rack refuses to go negative on an issue or a scrap** — it is one of the few places this system blocks rather than warns (A6) | a warning is only useful if somebody can act on it. A stack reading −4 cannot be acted on: nobody can use that count again until somebody works out which of the last twenty movements was wrong. The refusal names the way through — an opname with a reason — which is how a real surplus gets recorded, and leaves a row explaining where the extra wood came from |
| 195 | 2026-09-12 | **An identity number is masked completely — every character, separators kept — and the only thing shown beside it is its length.** The visible-prefix form (`332006***********`) was offered and rejected | owner asked for whichever is cheaper. A NIK's first six digits are province, city and district, so a prefix tells the room where every employee is from, on a screen the whole of HRD can open — and it buys nothing, because there is one KTP per person and the row already says whose it is. Nothing here needs digits to tell two rows apart. The **length** is what is worth showing: a fifteen-digit NIK is a failed reading, which is a fact about the scan, not about the person, and it is said without a digit |
| 196 | 2026-09-12 | **The masked number is masked in the payload, not on the screen.** `EmployeeDocumentView` carries `doc_no_masked` and the real number is simply absent; it comes back only from `revealEmployeeDocNo`, which writes an audit row. And the revealed value is held in one component's state, never cached | if the list shipped the number and the screen hid it, the reveal log would record a click while the number sat in the browser for anyone to read — a log that makes the thing feel protected without protecting it, which is worse than no log. In Phase 1 the whole store is in the browser so this is a contract rather than a secret; the contract is what Phase 2 implements, and getting it wrong now is what would make the log a lie later. A cache would make the second look free, and the second look is exactly the one worth recording |
| 197 | 2026-09-12 | **Opening somebody's identity number goes in the audit log, not the activity log — amending D188's line between them: the audit trail holds *acts*, of which changing a row is the commonest, not changes alone.** It is counted on its own (`reveals`), never folded into `changes` | *who looked at Karjo's KTP* is asked months later, usually by Karjo, and the activity log keeps detail for thirty days. The audit trail is never deleted, which is the guarantee this question needs. Counting it as a change would have quietly inflated every recap the day the eye button shipped — and on its own it is the more useful figure: one reveal is somebody doing their job, fourteen in an afternoon is a question. **The audit row never contains the number** — a log of who read a secret that stores the secret has multiplied the thing it was protecting, in the one table nobody may delete from |
| 198 | 2026-09-12 | **Only identity numbers are masked** — KTP, KK, NPWP, BPJS. A contract number, a certificate number and an ijazah number stay in the clear | those identify a *document*; the other four identify a *person* and are enough on their own to open something in their name. Masking everything would be ritual, and a screen full of rituals stops being read — which is how the masking that matters comes to be clicked through without thinking |
| 199 | 2026-09-12 | **A number carries where it came from** — `extracted` from the scan, `typed` by a person, or `pending` because the file is filed and nobody has read it yet — and **`extracted` without an attachment is refused** | the owner's flow is upload → Drive → read the number out of it, so the number has a provenance and somebody will later trust it because of that provenance. `pending` is the state the flow actually needs: a scan on file with no number is *absent and expected*, not blank-because-nobody-cared. The refusal exists because the seeds made exactly the impossible claim first (F57) |
| 193 | 2026-09-12 | **The pay-rule book is read by HRD and changed by IT.** `savePayRules` needs `it` at `write`; reading the screen needs only `payroll.read`, and the form renders fully but disabled for everybody else | owner: *aturan gaji HRD hanya lihat, IT yang bisa menggantinya*. Not a statement about trusting HRD with numbers — they are the people who know them — but about the one piece of configuration that reaches every payslip at once. HRD proposes and IT writes the version, so two people see a change before it reaches anybody's pay. The note field was already mandatory (D173); now the person typing it and the person asking for it are different people, which is what makes the note worth reading |
| 194 | 2026-09-12 | **A nav item may name a second permission that also opens it** (`orPermission`). Exactly one screen uses it: the pay-rule book, `payroll.read` **or** `it.update` | its two audiences hold nothing in common. An IT account with no payroll grant is an ordinary account, and gating the screen on `payroll.read` alone would hide the rules from the only people allowed to change them — a lock whose key is in a room you cannot enter. It stays under **Payroll** in the menu, where its readers look, not under IT (D192) |
| 190 | 2026-09-12 | **The IT module is open to IT and to leadership, and the verb the owner used is enforced: leadership holds `it: read`, and administering the module stays at `admin`.** Reading the two trails is a read; rolling a day up is a write; purging is admin; so is moving anybody's grants — which until now had no gate at all | owner, answering the half of Q22 left open: *yang boleh baca module IT hanya IT dan pimpinan*. **Leadership is not derived from an authority and there is no `is_leadership` flag**: the two candidates for deriving it, `approve_goods` and `approve_funds`, are exactly what D24 forbids, and a stand-in appointed to approve payments while the Direktur travels would silently gain the right to read everyone's activity log for that week. So the grant stays the only door and the rule is written beside it, on the screen where a grant is made and on the catalogue where it is read |
| 191 | 2026-09-12 | **`requireModule` was not enough and is no longer the only guard.** `requireLevel(service, module, level)` checks how far the grant goes, and its refusal names the level held as well as the level needed | the gate asked only whether a door was open. Give leadership `it: read` under the old guard and they could purge the activity log — the one deleting call in the system — because nothing looked past the module name. The refusal message matters as much as the check: *needs admin, your account has read* is actionable, *forbidden* is not (A7) |
| 192 | 2026-09-12 | **`Aturan penggajian` moved out of the IT menu and under Payroll.** Its gate was always `payroll.read` and it is unchanged; the route is unchanged | once the IT heading means *IT and leadership only*, a screen inside it that HRD can open makes the heading untrue. A menu group is a claim about who the screens under it are for, and the cheapest way to keep that claim honest is to put each screen under the permission it actually uses |
| 188 | 2026-09-12 | **Two trails, two rules. The audit log is never deleted; the activity log keeps 30 days of detail and six months of daily recap** — and the daily recap is the one derived figure this project stores, because it has to outlive the rows it was computed from | owner (Q22): *activity log secara detil bisa dijaga dalam rentang 30 hari sementara tiap hari harus di rekap user ini ngapain aja seharian. Lalu rekapan harian ini di jaga selama 6 bulan*. They answer different questions and so they cannot share a rule: the audit log says what changed and is the evidence behind every figure in the system, so deleting a row of it would make a past number unexplainable; the activity log says what a person did with their day, which is useful for a season and becomes surveillance for a decade. The recap breaks D-derived-on-read (D63) on purpose and it is the only place that does: the detail is gone at day 31, so a recap computed on read would return zero for every day older than a month — a figure that is not missing but wrong, which is the one thing forbidden here |
| 189 | 2026-09-12 | **Purging is the only deletion in the system, it runs as a rule rather than as a correction, and it refuses any day that has no recap yet** — `purgeActivity` returns the blocked days by name instead of taking the nearest number | every other removal in this system is a VOID that leaves the row (D9). This one genuinely deletes, so it is fenced: it deletes only `activity_events`, only past the retention edge, and only for days already summarised. Rolling up is therefore a precondition and not a convenience — a purge that ran first would destroy the day and answer *0 aktivitas* about it forever. The screen shows the count of days in that state (*5 hari punya detail tapi belum punya rekap*) before either button is pressed |
| 1 | 2026-09-10 | **Phase 1 is a frontend on demo data, deployed to Vercel.** No database until it is walked and signed off | ADR-009. The rules were never specified; you find them by walking a workflow, not by designing a schema |
| 2 | 2026-09-10 | Keep this repo's design system unchanged — shell, tokens, components, two keyframes, menu-as-data, details-in-a-drawer | it is finished and it is good; restyling would spend the fortnight on nothing |
| 3 | 2026-09-10 | Procurement and accounting only. Everything else keeps its honest placeholder | leanest path; those two are where the money is |
| 4 | 2026-09-10 | The demo layer implements `03-api.md` exactly — envelope, outcomes, 403/409/422, idempotency, latency | so Phase 2 replaces a module instead of rewriting screens |
| 5 | 2026-09-10 | Five services, separated by folder, schema, role and contract; one process at first | ADR-001 — "separate API per service so we can re-attach a third service later" |
| 6 | 2026-09-10 | One language, TypeScript | ADR-003 — `john-lau` pays a two-language tax on every reference list |
| 7 | 2026-09-10 | Enforcement in the database (RLS) once there is a database | ADR-002 — an API is about to exist, so screen-level gating stops being enough |
| 8 | 2026-09-10 | Postgres mints every identifier | ADR-005 — settles "who owns the `trx-` number" by having only one candidate |
| 9 | 2026-09-10 | No spreadsheet concept in the schema. Sheets become a one-way export | D1 — eleven schema places currently mirror sheet geometry |
| 10 | 2026-09-10 | The database owns every number, balances included | D9, and the owner's own 2026-08-28 direction, now with no legacy to reverse |
| 11 | 2026-09-10 | Keep the topbar role switcher in Phase 1, as a labelled demo control; delete it in Phase 2 | in a demo, switching role is how you show permissions work |
| 12 | 2026-09-10 | `john-lau` and `ops.talaliving.com` keep running, untouched. No data migration in Phase 1 | v2 can be wrong without costing anyone a day's accounting |
| 13 | 2026-09-10 | `02-database.md` gets rewritten on D14 against `findings.md` | it was written before we walked anything |
| 14 | 2026-09-10 | **Evidence is attached from the record it belongs to** — from a PR line or a ledger row — never matched to it afterwards | ADR-010. Today every document is an orphan looking for a parent, and a human reconstructs the linkage from name and amount proximity. That is why tracing is hard |
| 15 | 2026-09-10 | A context-free upload is the **exception road** only: bought first, approved later. It keeps its own inbox and its size is watched | the guess is unavoidable there, and only there |
| 16 | 2026-09-10 | **Google Chat is notification, confirmation, and the interface for people without web access** — not the intake door for evidence | owner, 2026-09-10. Chat is good at reaching someone who will never open the web app; it is bad at recording what a document belongs to |
| 17 | 2026-09-10 | A Chat action is a real action: the bot calls the same API as the browser, as the identified person. **Channel membership is never authorization** | the rekap's rule survives and sharpens — a *person* in Chat may act within their role; the *bot* may not act at all |
| 18 | 2026-09-10 | One document covering several parents is a **first-class action** (*juga mencakup…*), not a repair | it is why 674 files sit parked today as unnameable |
| 19 | 2026-09-11 | **PR approval belongs to one authority: the CEO.** Not "any director", no value threshold, no second tier | owner, answering Q2 |
| 20 | 2026-09-11 | **The IT gate is removed.** There is no routing step before the CEO | owner, answering Q4 |
| 21 | 2026-09-11 | **No urgency field.** Every requested line stays in the approval queue until it is approved, rejected or withdrawn | owner, answering Q4. A queue that shows everything outstanding needs no priority column — the CEO is looking at the whole list either way |
| 22 | 2026-09-11 | **Ledger visibility = accounting-module access**, not "everyone signed in" | owner, answering Q3 |
| 23 | 2026-09-11 | **A user holds several module accesses** — procurement + accounting + HRD is normal. Access is per module, per user | owner, answering Q3 |
| 24 | 2026-09-11 | *(a fifth authority, `approve_overtime`, was added on D145 — the model is unchanged, the list grew.)* **Module access and authority are separate grants.** Four authorities at the time: `approve_goods` (CEO), `approve_funds`, `post_ledger`, `resolve_inbox` | inferred from 19 + 23. It also fixes a real `john-lau` bug: the screen could not see who was allowed to post, so it offered a button the bridge then refused |
| 25 | 2026-09-11 | **Service lines need no receiving report** — payment proof completes them | owner, answering Q9 |
| 26 | 2026-09-11 | **Auto-complete ships inert.** Fuel and utilities get marked complete by hand for now; the rule is turned on later as a data change | owner, answering Q9: "for now lets leave it manual" |
| 27 | 2026-09-11 | **No custom domain in Phase 1.** The Vercel URL is the review surface | owner, answering Q12 |
| 28 | 2026-09-11 | **Approval is a checkbox.** Approved, or not yet. `HOLD` and `REJECTED` are gone; the ladder drops from nine values to eight | owner, answering Q19. Nothing is lost: an unchecked line stays in the queue exactly as `HELD` did, and an unwanted line is removed rather than rejected. The middle state existed only because the sheet had a checkbox *and* a status column |
| 29 | 2026-09-11 | **A line is removed because it is no longer needed.** No deadline, nothing ages out. Soft, audited, and **refused once money has reached the line** | owner, answering Q17. Past that point the words are return, credit or void — never a quiet disappearance |
| 30 | 2026-09-11 | **No substitute for the CEO.** A co-CEO grant can be added later if wanted; not designed now | owner, answering Q18 |
| 31 | 2026-09-11 | **Approval metadata is timestamp, name and email**, captured from the session. Assume the CEO acts from his own account | owner, answering Q19 |
| 32 | 2026-09-11 | **Attaching a payment proof to a line with no transaction offers to post one**, prefilled, on the same panel | owner, answering Q14 |
| 33 | 2026-09-11 | **Chat approvals cover goods and receiving, not fund decisions.** Approving a round stays on the web | owner, answering Q16 |
| 34 | 2026-09-11 | **The interface is English by default.** Multi-language later, not now | owner. Supersedes the earlier "English structure, Indonesian labels" mix (Q11) |
| 35 | 2026-09-11 | **Domain vocabulary that is *data* stays verbatim** — account codes, transaction types, unit codes, status strings, id formats, vendor and item names | translating a stored value does not translate it, it breaks the match |
| 48 | 2026-09-11 | **A purchase request is a collection of items, and the LINE is the unit.** A document is a submission batch, not a subject; one request may span several suppliers | owner. A request with one line paid and one still waiting has no honest single status, so the primary screen lists lines |
| 49 | 2026-09-11 | **`purpose` moves from the document to the line** | owner. Each item can be for a different job, so a purpose on the container would have to be wrong about most of them. It is also the field that turns a price into a decision |
| 50 | 2026-09-11 | **Carry-forward is not needed and will not be built.** A line stays on the board until settled or removed, so it "reappears at the next meeting" by never having left | the old CARRY/RETRO machinery existed because the surface was a spreadsheet with one tab per submission. Nothing has to be carried when nothing was filed away |
| 51 | 2026-09-11 | **Draft lines are editable; submitted lines are not.** After submission the routes are: reduce the approved amount, or remove and ask again | editing in place would rewrite what the approver saw |
| 52 | 2026-09-11 | **Evidence attaches from the line**, including for a line waiting for payment | ADR-010, now reachable rather than merely designed |
| 60 | 2026-09-11 | ~~**Navigation filters on authority as well as module access.**~~ **Superseded by D67** the same day: with approvals merged into the board there is no screen whose whole subject is one decision, and `NavItem.authority` was removed rather than left as scaffolding for nothing. `NavItem.authority` hides a screen whose whole subject is one decision | D24 in the menu. Procurement access should not put the CEO's queue in somebody's sidebar; the page itself stays readable, without controls, for anyone with `procurement.read` |
| 61 | 2026-09-11 | **The approval screen carries a "decided recently" list with un-approve**, not only the queue | a decision that can be made and not unmade is a trap: the only fix for a mis-tick would otherwise be database access (F9) |
| 62 | 2026-09-11 | **Un-approving a line money has already reached is allowed, and says what it will do** | the honest result is a line reading "paid, not approved" — a corner the board keeps in view. Refusing the un-tick would only mean the record disagrees with the CEO (A6) |
| 63 | 2026-09-11 | **No bulk "approve everything in this submission".** Each item is one tap | the purpose field is the reason approval is a decision rather than a rubber stamp, and a bulk button skips reading it. Revisit if a real queue is long enough to make this cruel |
| 91 | 2026-09-11 | **One `<EvidenceStrip>`, used on the request line and on the ledger row.** Attaching is the same act from either end (ADR-010) | two copies of the same block had already drifted apart once — the ledger drawer had the duplicate warning, the line drawer had the payment-proof handoff. A road that is the same road is one component |
| 92 | 2026-09-11 | **A document can be pointed at several records, from the record — never uploaded twice.** The candidates are offered (sibling lines of the same submission, the lines a payment settled) and never guessed | one invoice covering three deliveries is a fact about the invoice. Three uploads of the same photograph are three files nobody can tell apart later |
| 97 | 2026-09-11 | **The unit of the PO screen is the VENDOR, not the order.** One block per supplier: every order, the payment history across all of them, and the delivery history with its tanda terima | owner's sketch. A supplier with three open orders and one transfer that closes all three cannot be read order by order — each of the three would look like a partial payment that never completed |
| 98 | 2026-09-11 | **An over-delivery is a credit with the vendor, never value received.** `value_received` is capped at what was ordered; the excess is priced and named | owner's note on the sketch: *the two extra sheets are a small vendor credit, not applied to any order here*. Counting them would quietly turn an unasked-for delivery into money the vendor can invoice |
| 99 | 2026-09-11 | **Billable now** = the deposit share once the order is *issued*, plus the delivered share of everything else, minus what has been paid — floored at zero, per order | it answers the only question a supplier conversation turns on: what is safe to send money for next. A draft PO bills nothing however large the contract; paying ahead of delivery is exposure on the order, not a negative number here |
| 94 | 2026-09-11 | **Rejecting a document is the opposite of posting it.** Reject and note both keep the file, both demand a sentence, and neither writes a ledger row | asked directly: does the inbox record rejected chat uploads in the ledger? No — rejecting is how you say *no money of ours moved here*, and the file stays so that decision can be read months later |
| 95 | 2026-09-11 | **A retro request line is written unapproved**, then paid and allocated | it should read on the board as *paid, not approved*, because that is what happened. Writing it pre-approved would launder a purchase nobody authorised into one that looks ordinary |
| 96 | 2026-09-11 | **The extraction's vendor is offered, never assumed** — pre-selected only on an exact name match, with what was read shown beside the field | a near-miss silently picking the wrong supplier is worse than an empty field, and retyping a name already on the screen is how people stop using the queue |
| 100 | 2026-09-11 | **An order can be placed from the tracker.** `Add new PO` takes a vendor, lines with a unit price each, an optional deposit, and issues it — with a checkbox to leave it a draft instead | the tracker is where somebody notices a supplier has nothing open or that a job needs a second batch. Sending them to another screen to start again loses the moment, and a PO nobody sent is a document, not an obligation, so issuing is the default and drafting is the exception |
| 101 | 2026-09-11 | **A delivery is recorded with both halves of its evidence** — the photo of the goods *and* the signed tanda terima — plus how many, in what condition, who received it and who checked it. The form refuses without both | owner: *delivery history selalu ada foto item dan tanda terima barang, penerima, yang memeriksa barang / QC, jumlah dan kondisi*. They answer different questions: the photo says what arrived, the tanda terima says we acknowledged it. Three weeks later the argument about forty-seven versus forty-five is settled by whichever one exists |
| 102 | 2026-09-11 | **The tracker opens with total obligations across every supplier** — contracted, paid, owed, and of that, billable now — and **fully settled vendors are kept in a separate collapsed card** | the number nobody could get from the sheet is the company-wide one, and it is two numbers, not one: what we are committed to, and the part a supplier could invoice today. Settled vendors mixed into the same list are two thirds of the rows needing nothing, which is how a list stops being read — collapsed, not deleted, because last month's questions arrive next month |
| 103 | 2026-09-11 | **A vendor opens as a full-width page of its own** (`/procurement/tracker/[vendor]`), not a panel beside the list and not a row that expands under it | asked directly which is better. A vendor's story is three stacked wide tables — orders with six columns, deliveries, payments — so a side panel turns every one of them into a horizontal scroll on the laptops this is read on, and an inline expansion buries the list under one supplier. A page also gives the vendor a URL somebody can paste into chat, which is how half of these questions arrive |
| 104 | 2026-09-11 | **The sandbox signature carries a hand-bumped `FIXTURE_VERSION` alongside its automatic parts** — amending D90, which rejected one | D90 was right that a version number is a thing to forget, and wrong that shape and reference data are enough: two delivery photos moved from the PO to the receipt they document, and a saved snapshot kept showing *no photo* because nothing in the shape had changed. Row counts cannot do this job either — a visitor who records one delivery changes every count, and their work is what the snapshot is for |
| 105 | 2026-09-11 | **Payment rounds and the documents browser are parked — kept, not deleted, not developed further.** Both stay on the menu and keep working; no more hours go into either | owner: *sepertinya payment round dan document itu fitur yang tidak perlu tapi jangan hapus, biarkan saja dulu*. The two questions rounds exist to answer are already answered where the decision is made — the meeting board says how much to move into BCA 271, and the tracker says what is safe to pay a supplier next. Deleting them would throw away working screens over a judgement that may change, so they are frozen rather than removed, and M14 decides whether Phase 2 carries them at all |
| 106 | 2026-09-11 | **No money arrives from a client. The only inflow is operating funds from leadership**, and the liquidation report is per *transfer*, not per month: what was spent in the window between one transfer and the next, against what was sent | owner: *kita tidak menerima dana dari klien langsung hanya dana operasional perusahaan, makanya pimpinan minta kalau sudah di transfer 100 juta misal, uangnya kemana aja kok sudah habis*. The demo had a client payment in it, which taught a business model that does not exist; corrected. Nothing traces individual rupiah — where spending passes the transfer, the screen says the excess came from the balance already there, which is true and is what people want to know |
| 107 | 2026-09-11 | **A transaction type nobody has classified is treated as needing a decision behind it** — on the read side, where it is a flag | if an unknown type defaulted to *not expected*, the way to make spending escape the check would be to type a category that does not exist yet. `EJO` and `PACKING` are carried as-is rather than folded into `OTHERS` (Q10), so unknown types are normal here. The posting rules are unchanged: refusing a post over an unclassified type would block work, and this system warns rather than blocks |
| 108 | 2026-09-11 | **`/accounting/cashflow` is not built.** The ledger already carries the cash position and every row | owner: *bukankah cashflow itu sudah cukup diwakilkan oleh ledger?* Yes. A third screen showing the same five balances would be the next thing parked, so the placeholder is replaced by the liquidation report rather than joined by it |
| 109 | 2026-09-11 | **The payment calendar is a budget and a reminder in one**: one estimate per component, repeated every month, each carrying **the day it is due**. Twelve months forward, planned above and actual below | owner: *1 komponen tiap bulan. kita punya daftar tagihan berulang bulanan beserta tanggal pembayarannya. jadi selain jadi budgeting juga pengingat pembayaran*. Two screens would drift apart; the same list sorted by due date is what gets the electricity paid, and the same list summed by month is what says whether the year works. A month that differs is an override with a required reason — never a re-typed estimate, which would move every month after it |
| 110 | 2026-09-11 | **Two calendar lines may not claim the same ledger category.** Adding one is refused, naming the line that already has it | the category is how the plan finds what actually happened. With two lines on `RECCURING - PAYROLL`, no ledger row can say which one it paid, and the same money shows up twice in a plan somebody is about to make a decision on |
| 111 | 2026-09-11 | **The plan reconciles to the ledger, and the difference is a row on the screen**: *not in the plan* — everything that actually left in a month with no line claiming it | a budget that only shows what was budgeted is a wish. The demo's own September has Rp 2,2 juta sitting in that row, which is the honest state of a first month and the thing that makes next month's estimate better |
| 112 | 2026-09-11 | **The calendar never posts a transaction.** Money is recorded in the ledger, with its evidence, and *named* here afterwards | a screen that could write its own payments would be a second set of books nobody reconciles. Matching by category is a guess and the screen says so (`≈`); linking a row is somebody deciding, and it wins |
| 113 | 2026-09-11 | **A calendar line has one of three shapes: weekly, monthly, or once.** `amount` is **per occurrence**, not per month, and a one-off carries a real date and exists in that month only | owner: *tambahkan tagihan yang hanya ada di bulan tertentu — pengeluaran yang pasti akan ada tapi hanya saat itu, contohnya pembayaran vendor dan pelunasan kartu kredit bukan cicilan*. Modelling those as a monthly line would put them in twelve months instead of one. And payroll, written as one monthly figure, was hiding a real difference: it goes out every Friday, so a five-payday month costs Rp 150 juta and a four-payday month Rp 120 juta. Counting the days moved the year's failure from December to **November** |
| 114 | 2026-09-11 | **An override on a weekly line is the month's total, and the difference lands on the last run** | the THR is paid with one payday, not spread across four. December: three runs of Rp 30 juta and one of Rp 70 juta, and the run that carries it says so on the row |
| 115 | 2026-09-11 | **A month opens into a timeline of its dated movements with the balance running down** — naming both the **first day it goes under** and the **lowest point** | reading by month silently assumes the money in arrives before the money out. December opens under, is rescued by the transfer on the 3rd, and goes under again on the **18th** — five days before the figure the month view shows. Which day it breaks decides the fix: move the transfer earlier, or change what is being bought. Not a calendar grid: 30 cells with six marks is worse to read than the list it replaces |
| 116 | 2026-09-11 | **The due panel and the month expansion are the same events**, from one derivation | they are the same list read two ways — one filtered by date, one grouped by month. Built separately they would drift, and the one people trust for reminders would be the one nobody maintained (the reasoning of D91, applied again) |
| 117 | 2026-09-11 | **The guided walk is a bar along the bottom on the real screens, never a spotlight over a dimmed page** — `?tour=flow-b&step=n`, so it can be pasted into a chat and leaving it is closing a bar, not escaping a mode | half of what this demo is for is people clicking things nobody planned for them to click, and a tour that dims the rest of the screen turns that into a locked door. Every step says what to look at *and why it matters*: a tour that only points is a worse version of the menu |
| 118 | 2026-09-11 | **The dashboard reads the demo store, like everything else.** The shell's sample page — invented sales orders, a timber-yield chart — is gone | it was defensible while it proved the design system worked on realistic shapes, and indefensible once every other screen read real derivations: the landing page was the only place in the app that could not be trusted, and it is the first thing anybody sees. It now answers, in order: how much money is there, does it last, what falls due, what waits on a decision, what is stuck — each linking to the screen that can act on it |
| 119 | 2026-09-11 | **Q20 settled — the CEO may still approve part of a line.** The approved amount starts at what was requested and may be changed before the box is ticked, in either direction | walking it settled two halves of one question. Reducing survives ("approve two of the five you asked for"); and the cap that stopped it going *up* was removed (D76), because prices move between the request and the meeting and a wall reads as a bug |
| 120 | 2026-09-11 | **Q23 settled — an overpayment, and an over-delivery, are stated and never netted.** No vendor-credit account is created | the same shape twice: Rp 225.000 paid above approval, and two sheets delivered above order. Both stay visible on the record with their explanation until a person decides. A credit account would be a place for them to go quiet, and it is additive later if the owner asks |
| 121 | 2026-09-11 | **Q15 settled — `finance` and `it_admin` resolve the exception inbox**, matching who may post, with the resolver's name on the resolution either way | built and walked; nobody wanted a narrower list, and a wider one would mean decisions with no accountable name |
| 122 | 2026-09-11 | **Q6 and Q8 settled in code** — `shared@` posts to the ledger, and `WAITING FOR APPROVAL` is the canonical spelling | both were already the owner's answer; recording them here so the next reader does not re-open them |
| 123 | 2026-09-11 | ~~`APPROVED` and `WAITING FOR PAYMENT` both mean approved-and-unpaid, so the screen states what separates them: whether the cash is already in the paying account~~ **Superseded by D126 the same day** | the answer was wrong in a way the question had already hinted at. There is no difference worth showing, because the thing it claimed — that the cash is there *for this line* — is not true |
| 126 | 2026-09-11 | **`WAITING FOR PAYMENT` is removed from the ladder. One value covers approved-and-unpaid: `APPROVED`, read as *approved, not paid yet*.** An approval is a **claim** on the account, never a reservation against it | owner: *uang yang sudah dianggarkan bisa jadi dipakai untuk item approval yang baru, sehingga item approved lama tidak ada anggarannya jadi nominal uangnya harus diajukan kembali*. Cash is fungible — money transferred in for last week's approvals is spent by whichever payment is made first — so a status meaning "the money is waiting for this line" is a promise the system cannot keep. Seven values in the ladder, down from eight. Where the account cannot cover what is approved, the board says so in those words and says why the shortfall can come back |
| 124 | 2026-09-11 | **What is being approved and what still has to be paid are two different numbers.** The picked total counts only what is left to pay; a line already paid contributes nothing to it | owner. A line bought first and approved later commits no new money — approving it records a decision about money that has gone. Both numbers are shown when they differ, and the already-paid part is named, because the approval figure is still the real figure for the decision |
| 125 | 2026-09-11 | **A request for payment must carry what stands behind it — a shop link, an invoice, a bill, an order — and is refused without one.** A **link is first-class evidence**: same table, same road, same strip | owner: *setiap pengajuan untuk pembayaran harus dilengkapi dengan dokumen pendukung, misal link purchase toko online, invoice, bill*. At request time the nota usually does not exist yet and the marketplace page is what the price came from; photographing that screen would turn it into a file and lose the only part somebody else can check. Refused rather than warned: approving a bare number is the habit this system exists to end, and the fix takes ten seconds. Un-approving is never blocked |
| 127 | 2026-09-11 | **The meeting board carries an instruction column on every row** — typed while the item is being discussed, not behind a click. On a line still waiting it rides along with whatever happens next: recorded as leadership's instruction when approved (D64), or carried to the approver's chat card as *the meeting's words*, prefilling their field. On a line already approved it is saved on its own, which only an approver may do | owner: *meeting board harus menyediakan kolom instruksi untuk tiap baris*. An instruction is said out loud while the item is on screen, and anything needing a click is said and then lost. The split matters for attribution: a staffer typing what the room said is not leadership deciding, so their text travels as context and only becomes an instruction when the approver sends it back — visibly, from a field they can edit |
| 128 | 2026-09-11 | **A payment term is a trigger plus a share, and money that reached an order is applied to its terms oldest first.** A term whose trigger has fired while an earlier one is unpaid is `BLOCKED`, and the screen names the term holding it up | nothing in a bank transfer says which term it was for, so ordering it oldest-first is both the only defensible reading and how the terms were meant to run. What falls out is the guard the old system never had: a final instalment cannot quietly go out on an order whose deposit never did. The demo carries that exact state — an overhaul delivered, the 50% deposit never sent, so the progress payment reads BLOCKED by `-M01` |
| 129 | 2026-09-11 | **An issued order only moves by amendment: the old line stays and points at the new one, and the reason is required.** Receipts follow the live line | *what did we agree, and when did it change* is asked with a vendor on the phone, months later. Editing in place answers neither. The reason is mandatory for the same cause — the numbers say what changed, only a person can say why. A delivery already recorded moves with the line, or it would vanish from the order it arrived against |
| 130 | 2026-09-11 | **Closing an order is refused while either axis disagrees, or while nothing is filed against it — and a written reason is the way past that** | money and goods are never collapsed (A1), so "finished" has to be true of both. But real orders end untidily: the last two sheets never came and nobody will chase them. What is not acceptable is an order closing as though it had finished cleanly, so the settlement reason goes on the audit row and is what somebody reads in six months |
| 131 | 2026-09-11 | **Receiving is two acts: a report and a confirmation** (answering Q28, and superseding half of D101). Anyone who was there records the arrival with a **photograph** — no tanda terima needed. Procurement, who are accountable, complete it with the signed tanda terima, the QC name and the daylight count. **Only a confirmed receipt counts as value received** | owner: *ada tim procurement yang punya akses, masalahnya item yang dikirim dari luar biasanya datang di luar jam kerja jadi mereka harus lapor, sementara tanda terimanya bisa menyusul*. D101 required both documents, which was right about evidence and wrong about time: refusing the report until the signed paper exists does not produce the paper, it loses the arrival. The split keeps both — the arrival is recorded at 23:40 and visible everywhere, and it moves no number until somebody has acknowledged it in writing. The audit row carries how many hours the paper took, which is the measure of whether this road is working or being used to avoid the paperwork |
| 132 | 2026-09-11 | **An order is confirmed by leadership before it is sent, so creating one cannot also send it.** A new PO is always a DRAFT; `issue` is refused without the confirmation | owner: *PO harus bisa di mintakan konfirmasi ke pimpinan untuk mendapatkan approval*. A purchase request is a request to spend; a purchase order is a promise made to a supplier in the company's name, and the two are not the same decision. This narrows D100, which made issuing the default — right about a draft owing nothing, wrong about who gets to make the promise. *Waiting on leadership* is its own visible state, because "it is sitting with the boss" is where orders actually stall |
| 133 | 2026-09-11 | **The vendor's PDF is the app's own print view, not a generated document** — `/procurement/po/[po]/print`, laid out for A4, printed to PDF by the browser, then sent on WhatsApp through a `wa.me` link prefilled with the order | one renderer, not two: a PDF library would be a second description of the same order, free to drift from the first. It shows only what a supplier should see — what is ordered, at what price, when it is expected, the terms — and never our exposure, what we have paid, or who approved it internally. The app shell is hidden when printing, because a purchase order must not carry our menu |
| 134 | 2026-09-11 | **A purchase order carries the delivery date the vendor promised**, and moving it after the order is sent needs a reason | owner. Without a promise nothing is late, it is merely absent — and *late* is the word that starts a conversation with a supplier. The date is what the vendor said, not a rule that derives one, so changing it is a fact about them and is kept with what they said |
| 135 | 2026-09-11 | **A sent order is amended, never edited — and amending it does not go back to leadership.** What it does do: a confirmed *draft* loses its confirmation when the numbers change, and an *issued* order gains a **revision number** and reads *changed, not re-sent* until somebody says they sent it | owner, asked directly: re-confirmation *never*. The reasoning holds — the vendor already has the order, so asking leadership again changes nothing they hold, and routing every corrected typo past the CEO is how an approval becomes a rubber stamp. But a confirmation is a yes to particular numbers: change them before it is sent and the yes refers to something nobody saw, so it falls away. And once it *is* sent, two pieces of paper exist — the revision on the PDF is what lets them be told apart |
| 136 | 2026-09-11 | **HR is a sixth service (`hr`), not part of `identity`** | both are about people and they answer to different rooms: identity says who may open a screen, HR says what somebody is owed. Payroll is the most access-controlled data in the company and the most likely to need its own retention rule; merging it into the table that holds logins makes both harder to reason about |
| 137 | 2026-09-11 | *(amended by D141 — the day is no longer "closed", it is read, and a missing tap is added rather than a day completed. What survives is the rule underneath: nothing is guessed, and a time somebody types carries a reason and their name.)* **A day the machine did not describe is worth nothing until a person reads it.** Nothing ever guesses the missing stamp | the fingerprint machine produces times, not days, and a missing check-out is the normal failure — a dirty sensor, the workshop door, a power blink. "Assume they left at five" is how a payroll quietly pays for a day nobody can account for. The reason is what separates a correction from a favour three months later, and it carries the name of whoever typed it |
| 138 | 2026-09-11 | **Overtime is claimed and approved, never inferred from attendance** | the machine knows somebody was in the building at 19:40. It does not know whether they were working, waiting for a lift, or finishing a cigarette, and paying 1,5× for all three is how overtime becomes a habit rather than a decision. Hours past the standard day are **shown** the moment they happen and **paid** only once a supervisor says they were worked |
| 139 | 2026-09-11 | **A payroll run cannot be approved while any day in its period is open**, and payroll lines are computed on read, never stored | a payroll over days nobody finished recording is a number that looks exact and is not — and the people it is wrong about are the ones paid by the day, who are least able to argue. Storing the lines would be worse: a figure that disagrees with the days behind it is somebody's wages being wrong, found out when they count their money |
| 140 | 2026-09-11 | **Payroll computes gross and stops.** BPJS Kesehatan, BPJS Ketenagakerjaan and PPh 21 are not modelled, and the payslip says so in Indonesian | none of them has been described to us: which apply here, at what rate, who pays which half. A deductions block full of zeroes reads as *nothing was deducted*; a payslip that states it computes gross reads as *this part is not done yet*, which is the truth. A missing deduction is obvious; a wrong one is discovered by somebody who is short (Q30–Q32) |
| 141 | 2026-09-11 | **Attendance is stored as taps, not as days.** One row per scan on the reader, exactly as the machine exports it; the six slots — *masuk, istirahat keluar, istirahat masuk, pulang, lembur mulai, lembur selesai* — are a **reading** of those taps, computed on read. Anything the reading cannot place leaves the day in `review`, never a guess. Amends D137: a day is not "closed", it is **read** | the owner's own export was the evidence. 981 taps, 35 people, ten days: 29 double taps inside two minutes, and after removing those, **48 days out of 227** still had an odd number — counts of 1, 2, 3, 5 and 7. Karjo on 31/08 taps at 07:21, 12:02, 12:33, **12:48**, 17:32, 17:58, 19:59; Roni on 30/08 taps three times and never again. A model with a `check_in` and a `check_out` column cannot hold that file without throwing information away, and the information it throws away is the part somebody has to look at. Storing what the machine said, and deriving the rest, means a wrong *rule* is a one-line change rather than a re-import |
| 142 | 2026-09-11 | **HRD marks a day for what it was, and marking never touches a tap.** Six kinds — *tanggal merah, setengah hari, tidak masuk, sakit, cuti, izin* — each with a reason and a name. A mark with no employee covers the whole office. Payroll reads a day's **value**: 1 ordinary, 0,5 for *setengah hari*, 0 for the rest; on a *tanggal merah* the hours worked become lembur and the day itself counts nothing | the machine cannot know that the power went at one o'clock, that it was Maulid Nabi, or that somebody was ill — those are facts about the day, not failures of the reader, and correcting the scans to express them would destroy the only record of what actually happened. Keeping them apart means the taps stay evidence and the mark stays a decision, each with its own author. A public holiday is a property of the date, so it is marked once rather than forty times |
| 143 | 2026-09-11 | **An import never creates a person.** A machine number the system does not know is reported back with its tap count; the taps are left out until somebody registers that number against a name | the export's `No.` is the machine's own numbering — the file the owner sent has numbers 6 to 138 with gaps, and no reliable name spelling. Creating a row from an unknown number would put somebody on a payroll who was never hired, and the cheapest moment to notice a stranger in the file is the moment it is uploaded. Re-uploading the same file is a no-op: a tap is who and when, to the second |
| 144 | 2026-09-11 | **A marked day is paid only when something says it should be: *sakit* with the surat dokter attached, *cuti* out of that person's own balance.** Everything else — izin, tidak masuk, cuti past the entitlement, sakit with no letter — is recorded and not paid. The entitlement is a per-employee number, and the days used are **counted from the marks**, never stored as a remaining balance | owner, answering Q33. Two things follow from taking the answer literally. First, the letter is *evidence*, so it goes on the same road as every other document here (ADR-010) — uploaded and linked to the mark, by name, with a time — which also means it can arrive late and the day turns paid the moment it does, no re-run and no correction. Second, the balance is derived: a stored *sisa cuti* drifts the first time a mark is removed, and the person it drifts against is the one who loses a paid day. The timesheet says of every day what it is worth **and why**, in Indonesian, because "why is this day worth nothing" is the question an employee actually asks, and an app that cannot answer it makes HRD answer it from memory |
| 145 | 2026-09-11 | *(narrowed by D146 — this is the rule for **production** overtime; a staff session needs HRD alone and is paid by default.)* **Production overtime takes two signatures: HRD, then leadership holding the surat lembur** — and `approve_overtime` is a fifth authority rather than a reuse of `approve_goods`. Leadership's approval is **refused** while the letter is not attached; HRD's is not, because the letter can follow the hours | owner, answering Q34. The two steps check different things, which is why one is not enough: HRD checks the hours against the taps — was he here, are these the hours the machine shows — and leadership checks the decision, was this work worth paying for. A separate authority because approving that a table arrived and approving that a man is paid for four extra hours are different decisions, and the people who hold them will not always be the same person (D24). The refusal is the point of the letter: signing without it is signing a number somebody typed |
| 146 | 2026-09-11 | **Overtime arrives as a sheet, and there are two kinds.** *Produksi*: one sheet, one night, **many names**, each with the job, the item, the stage and how many — signed by leadership. *Staff*: one sheet **per session**, one person, with their own work report (usually a screenshot) — **no leadership signature, and paid by default**; HRD decides only whether to turn it off, with a reason. Amends D145, whose two signatures now apply to production alone | owner, describing the paper that already exists. The two are genuinely different documents and treating them as one form with an optional field gets both wrong. A production night is a batch: forty names on one page, signed once, because that is how a workshop supervisor works. A staff session is a person and an evening, and its evidence is the work itself — a screenshot of what they were doing. Making a designer's Tuesday evening wait on the Direktur is how an approval becomes a rubber stamp, and defaulting it to *paid* is the honest position: they already stayed, the report is attached, and the decision left to make is whether to take it away |
| 147 | 2026-09-11 | **A signed production sheet is also the production report.** Each line carries the work order, the stage and the quantity; leadership's signature posts them to the production board as progress entries, keyed by sheet number so a repeat is a no-op. The posting is composed at the screen (ADR-004) and authorised by the signature itself — `approve_overtime` is accepted by `production.recordProgress` for exactly that source | the owner described the sheet as carrying *item apa dikerjakan, proses sampai mana dan berapa*. That is a production fact travelling on a payroll document, and the moment somebody writes it down twice the two copies begin to disagree — the workshop's version and HRD's version of the same Thursday night. Typing it once and letting the signature move both is the only arrangement where they cannot drift. The authority rule follows from the same thought: if the Direktur may sign the sheet, refusing the posting his signature causes would leave one act half-done |
| 148 | 2026-09-11 | **Production is a seventh service (`production`), built around the work order**: what is being made, how many, for whom, **by when** — plus append-only progress entries per stage. Seven stages, seeded as data. A correction is a negative entry with a reason, never an edit | it was the overtime sheet that demanded it (D147), but the board answers a question nobody could answer before: *which of the eleven things on the floor is the one that is late*. A workshop always knows what it is building. Progress is counted as stages finished across the whole quantity rather than as the furthest stage reached — eleven doors cut and one packed is a tenth of the way through, not "packing". Reporting more than was ordered is refused; a stage running ahead of the one before it is **warned** about and accepted, because refusing it would only mean the work goes unrecorded (A6) |
| 149 | 2026-09-11 | **Master data: a project is the customer's order, a product is what we sell and make, and a bill of material is a product pointing at catalogue items by code.** Products live in `production`, **not** in `procure.items`: those are things we *buy* and are half uncurated by design, a product is quoted and made and is always curated. A BOM line carries **qty and susut separately**, and a material cost is computed on read, never stored, and never completed by guessing | the owner asked for master data and the shape follows from what each thing is for. The project is the dimension every service hangs on — procurement buys for it, production makes for it, the ledger spends on it — and it was four fields, enough to tag a row with and not enough to say whose order it is or when it was promised; its **code** is the part that must never move, because every one of those references is text at the seam (ADR-004). Waste is separate from quantity because the number in the drawing and the number to buy are different numbers, and conflating them is how a workshop runs out on a Saturday. The cost is derived for the same reason payroll is (A3), and a component with no price leaves the total marked *incomplete* rather than silently counting as zero: a cost that treats the missing ones as nothing reads as complete, which is the one way it can be worse than no cost at all |
| 150 | 2026-09-11 | **Every product carries a size in millimetres, a *gambar kerja* and a *gambar jadi*; every project carries the items ordered and how many.** The size is three numbers, not a sentence. The two drawings are two document kinds, on the same road as every other document, and a revision is a new file rather than an edit. An order line names the product **by code**, which is what lets *dipesan · dibuat · selesai* be read side by side | owner. Taking "harus ada" literally means the system must be able to say when it is **missing**, and a sentence cannot be checked — so ukuran is `length_mm`/`width_mm`/`height_mm` and the catalogue carries a completeness column naming what each product lacks. The two drawings answer different questions: the *gambar kerja* is what the workshop builds from, the *gambar jadi* is what the client was shown and what QC checks against; one field called "drawing" would silently lose whichever was filed second. The order lines are what turn a project from a label that spending gets tagged with into an **order**: the fixtures show four tables ordered and four made, ten doors ordered and twelve on the floor, and a signed job three weeks old with nothing started — none of which was visible before |
| 151 | 2026-09-11 | **A bill of material can be turned into a purchase request, and the request carries the work-order number it came from** (`pr_lines.source_wo_no`). That one column is what makes *proyeksi vs aktual* answerable: the BOM says what a run should need, the PR raised from it is what somebody actually went out to buy, and both are sums over the same rows. The PR is created as a **draft** — never submitted automatically — and the comparison is **materials against materials**, with labour in neither side | owner: *bisakah baris dalam BOM menjadi representasi dari PR, tujuannya di akhir project bisa membandingkan ongkos produksi aktual vs proyeksi*. Without the link the two numbers exist and cannot be reconciled: a PR raised in August for plywood is not obviously the plywood in this table's BOM, and matching them by item code afterwards is guesswork the moment two orders run at once. Draft rather than submitted because a bill of material is a **requirement**, not a decision to spend money — the list still has to be read, priced and asked for by a person. And the comparison stays narrow on purpose: the ledger's project total includes installation, delivery and subcontract, so subtracting it from a materials projection would produce a number that looks like an overrun and is a category error. That wider figure is shown, separately, saying what it contains |
| 187 | 2026-09-12 | **Geography is a market — country → region → city → the local label — not a flat `area` string**, and the label stays verbatim. A market carries its **currency**, its **time zone** and its outreach **language**, because all three are properties of the place. One filter works at three altitudes: the scope is a **prefix of the market code**, so `AU` is a country, `AU-QLD-GOLDCOAST` a city, the whole code one district | owner: *SP NORTH itu karena kita baru 1 negara; asumsikan kita scraping ke seluruh dunia*. A flat column of district labels is unusable the moment the scrape leaves one coast — *Seminyak* beside *SP MIDDLE* beside *Downtown Dubai*, with nothing to group or roll up by, and no way to tell whether two rows are the same city spelled twice. The currency is the part that would have gone quietly wrong: an ADR of 106 and an ADR of 1.850.000 are not comparable and not addable, so no figure in this module ever mixes them and no rate is invented to make it possible (the rule D181 already set). The time zone earns its place on the screen rather than in the schema: whether to ring an agent depends on what time it is **there**, and the queue now says so beside each name |
| 186 | 2026-09-12 | **Commission is computed from contracts that exist** — a project code and a value — and this module records what is **owed**, never pays it. Payment goes through the ledger like any other money | a referral that has been surveyed and quoted is work, not revenue, and a percentage of a hoped-for size is a figure the agent will eventually quote back at us. So `WON` is refused without both a project code and a contract value, and everything short of that contributes nothing to the total. Keeping the payment in the ledger is the same rule the whole system runs on: one road for money, with its own evidence and its own authority (ADR-006, D85) |
| 185 | 2026-09-12 | **An agent who agrees becomes a representative with a rate, and a DEAL cannot be recorded without one.** Onboarding creates the rep, links the agent to it, and from then on everything they introduce is counted against that percentage | the outreach funnel ends at DEAL; the programme *starts* there, and an ordinary CRM has no shape for the second half. The refusal matters more than it looks: a deal recorded against an agent nobody onboarded is a commission nobody can compute, discovered when the agent asks — which is the worst possible moment to be inventing a rate. The rate is per person because it is negotiated per person, and the range is guarded (0–20%) because it is a number that will be paid many times |
| 184 | 2026-09-12 | **A score from the enrichment is a machine's opinion until a person validates it**, and the screen says which it is | the pipeline arrives from outside — scraped from the map, enriched, scored — and every one of those numbers is somebody else's inference. The tracker already carries a `VALIDATED` column for this reason; keeping it means the team can act on unvalidated rows (waiting would stall the week) while never mistaking them for checked ones. The demo's Meriton has score 3 and an ADR flagged `CHECK` from a single source: exactly the row that looks like data and is not yet |
| 183 | 2026-09-12 | **The Package programme is the ninth service, and it models two funnels joined at one point**: reaching an agent, then working with the agent who agreed. The vocabulary of the tracker the team already runs is carried **verbatim** — `MSG SENT`, `FORM BACK`, `MOVE ON`, `SP NORTH`, `TL-0001` — and the seven-day move-on rule is **derived from the day the message went out**, never a column somebody maintains | owner, with the existing dashboard as the reference. Three things the spreadsheet cannot do and this has to. The funnel counts **properties by their furthest agent**, because a building where one agent is at DEAL is not also a building at QUEUED and counting it twice is how a funnel stops adding up. The move-on flag is computed, because a column somebody has to remember to fill in is a column that is wrong by Friday — the demo has an agent nine days silent that the sheet would still show as merely *MSG SENT*. And moving on is **one act**: recycle this agent and message the next, because doing half of it is how a property stalls with nobody chasing anybody. Scraping and enrichment stay outside; what crosses the boundary is a file, imported the way every other outside file is (D143) |
| 182 | 2026-09-11 | **A statement's own arithmetic is checked before anything is booked from it**: opening + Σ lines must equal the closing balance the bank printed, and the two balances are **typed from the header**, not parsed | a partial export — pages missing, a filtered download, a copy-paste that stopped halfway — looks exactly like a complete one once it is in a table. Asking for the two balances costs the person five seconds and turns the file into something that can check itself. It does not refuse the upload: a file that does not balance is still worth having, and hiding the disagreement is the only unacceptable option |
| 181 | 2026-09-11 | **A foreign-currency line cannot reach the ledger until somebody types the rate the bank gave that day.** The rate and the original amount stay on the row, and on the ledger row's remark | BCA USD 081 is one of the two leadership accounts, so dollars arrive here whether or not anybody has thought about conversion. Every automatic answer is wrong: a mid-market rate is not what the bank gave, yesterday's rate is not today's, and an average is nobody's. The advice from the bank has the real number on it. Until it is typed the line simply cannot be booked — which is a missing figure, and the alternative is a wrong one in the ledger that looks right on the screen |
| 180 | 2026-09-11 | **For the leadership accounts the rekening koran is not a reconciliation — it is how their rows come to exist.** Uploading one creates ledger entries, with the statement itself standing as the evidence (and so joining the primary document kinds, D85). A ledger row that already exists is **offered as a match, never tied automatically** | owner: *user upload rekening koran terutama untuk 064 dan 081, karena 2 ini di pimpinan dan tidak secara terbuka diberikan*. That last clause is the whole design: nobody in the office sees these accounts, so nothing else can produce their movements. The demo statement makes the point — four lines match transfers somebody already typed, and two do not: a bank charge nobody would ever enter by hand, and a payment leadership made straight to a timber vendor, which is precisely the spending that was invisible. Matching is offered rather than applied because two identical transfers in one week would otherwise reconcile against each other's rows in silence, and the error would be found by a bank, not by us |
| 179 | 2026-09-11 | **Desain is a queue, and uploading a revision is not releasing it.** A drafting task belongs to a product, revisions are append-only and numbered by the drafter, and only a **released** revision is what the workshop may cut from. A release is refused while any question on the task is unanswered. The queue is ordered blocked → revision-not-released → undrawn → by the date the job actually needs it | owner: *pastikan modul desain ini dapat mempermudah pekerjaan tim drafter*. The drawings already existed as documents on a product (D150); what did not exist was the work. Three things the queue can say that a folder cannot. **What is blocked** — a question asked ten days ago that nobody answered is somebody else's decision holding up a saw, and only the drafter can see it. **What the floor is actually cutting from** — revision C exists, revision B was released, and nothing looks wrong: there is a recent upload and a drawing on file, which is precisely why it has to be said. **What is not drawn at all** — computed from the orders and work orders, so putting a product on a job makes its missing drawings appear the same day rather than waiting for somebody to remember. Releasing over an open question is refused rather than warned because that is the one path that ends with a wrong thing built in wood |
| 178 | 2026-09-11 | **A day off is asked for, decided, and only then written onto the timesheet.** Approving a request writes the marks; a day that already carries one is **skipped, not overwritten**. Nothing is refused for running past the entitlement — the days are taken and recorded either way (D144) — but how many would be paid is shown **before** the decision | the system had only the second half: HRD wrote *cuti* on a day and payroll worked out whether it was paid. The half that was missing is where the argument actually happens. An employee asking on the first of the month wants one number — how many days are left — and a balance that counts approved-but-not-yet-taken days is the only version of it that is true. Skipping rather than overwriting matters more than it looks: a public holiday inside somebody's leave is still a public holiday, and a mark typed by a person at six in the morning outranks a batch write |
| 177 | 2026-09-11 | **Berkas 201 is a checklist, not a folder — and a document may be a number without a scan.** Required kinds are listed whether or not anything is filed, so the screen can answer *what is missing*; documents carry an expiry, so it can answer *what lapses soon* | owner. A shared drive holds the same scans and cannot answer either question. The checklist is the same move as a product's gambar kerja (D150): taking "harus ada" literally means the system must be able to name what is absent. The expiry half earned itself immediately — the fixtures put a forklift licence in the file that lapsed in July, and nothing anywhere would have said so. Allowing a number with no scan is deliberate: a BPJS registration asks for the number, not the card, and refusing to record it would push the number back onto a spreadsheet |
| 176 | 2026-09-11 | **Undertime is part of the gross; a deduction for lateness or an SP is not.** A short hour under a stated rule is arithmetic over recorded hours, so it belongs with base pay and overtime. What one person decides about another — keterlambatan, surat peringatan, kasbon — stays an adjustment with a name and a sentence (D155) | the two look alike on a payslip and are completely different in kind. One is recomputable from the timesheet by anybody; the other is a judgement that has to be defended. Mixing them would let a deduction somebody decided hide inside a total that looks computed |
| 175 | 2026-09-11 | **A rule change cannot be saved until its effect has been computed**, person by person, against a real period. The save button opens only after the preview has run | a multiplier is an abstraction until it is rupiah: *1,5×* means nothing, *Sumiati Rp 472.500 → Rp 448.537* means something. The preview applies the proposed book to the same days and the same approved sheets and shows only the lines that move. A pay rule saved without looking is a pay rule whose effect is discovered by the person whose wage it changed |
| 174 | 2026-09-11 | **Undertime ships off, and lateness stays manual.** The mechanism is built — per-hour, or a half-day step past half a day, with a grace in minutes — and the default deducts nothing | the owner named undertime as a scheme that exists here, but not what a short hour costs, and the difference matters: a deduction invented by software reaches somebody's pocket. So the rule is visible, switchable, and **previewed** before it is turned on (D175). Marked days — sakit, cuti, tanggal merah — are never undertime: they are different days, and counting them would deduct twice |
| 173 | 2026-09-11 | **Pay rules are dated versions, never edits, and a period is computed under the version in force when it opened.** Overtime follows the national ladder — 1,5× the first hour, 2× after, and a rest day on its own 2×/3×/4× ladder — with a monthly salary divided by 173 to reach an hour of it (owner, **answering Q31**). The GAJI written on the paper form still wins where it speaks (D154) | asked for: *skema gaji dan aturan harus customizable*. Three consequences follow from dating them and each one is the point. A payslip from March stays recomputable under March's rule, so a dispute can be settled by arithmetic instead of memory. A change cannot be backdated, because days already worked were worked under a rule somebody could have read at the time. And a version dated **inside** an existing run is refused rather than silently ignored — the payroll picks the rule in force when the period opened, so a mid-period version would look applied and do nothing, which is the worst of the three outcomes. The ladder is applied **per night**, because "the first hour" is the first hour of that night and not of the fortnight |
| 172 | 2026-09-11 | **Stock that arrived without a price is counted and left out of the valuation**, and the screen names it. The value that comes back is a value of *part* of the rack, marked incomplete — never a rack valued as if the unpriced part were free | the same rule as the BOM's material cost and the timber that has not been sawn: a figure is allowed to be missing, never allowed to be quietly wrong. Eight sheets of plywood arrived on a lump-sum line with no unit price; valuing them at nought would have made the rack look Rp 2,3 juta cheaper than it is, and nothing on the screen would have said why. **What is deliberately not answered here is the cost of stock that left** — FIFO, average and standard costing give three different numbers and nobody has said which this business uses (Q43) |
| 171 | 2026-09-11 | **An opname records the difference, not the new number — and the difference needs a reason and a location.** The form asks what was counted, shows what the system believes about **that rack**, and stores the gap. Counting exactly what the system said writes nothing | a form that asks for the difference invites somebody to type the number that makes the screen agree with itself. Asking for the count is asking for the only thing the person at the rack actually knows. A location is required for the same reason: writing a three-location total's difference against whichever rack sorted first would put a correction where nobody counted. And the reason is the whole value of the row — *fisik 186, sistem 204, dugaan terpakai di perbaikan tanpa dicatat* is a lead; "adjustment" is a shrug |
| 170 | 2026-09-11 | **Stock is a movement ledger, and confirming a delivery is what puts goods on the rack.** Nothing stores a quantity: on-hand is the sum of the moves, computed on read (A3). Issuing more than the record shows is **recorded and flagged, never refused** | the gap this closes is the oldest one in the system: a delivery was confirmed, paid for, and then forgotten until somebody walked to the rack — so nobody could tell whether the next request was needed. A stored quantity is the other failure, the one the spreadsheet already has: it disagrees with its own history and the disagreement is discovered by a man standing in front of an empty rack. On refusing: the wood is either there or it is not, and a screen that will not record what a storeman just carried out teaches him to stop recording. What it must not do is stay quiet — a negative figure is shown as a counting problem to resolve, which is what it is |
| 169 | 2026-09-11 | **The catalogue is filed two levels deep, and whether something is *counted* is a property of its category**, not a flag on each item. Jasa, electricity and anything nobody has filed yet are bought and used, never stocked | the first version of this list had nine flat headings and one of them was "Production", which is every item in the workshop. A category earns its place by separating two questions somebody actually asks — *how much wood is on the rack*, *which finishing is running out* — and anything that separates neither is a word, not a category. Two levels and not three because the parent is what a report groups by and the child is what a storeman looks for; a third level would be the item itself. Putting `stocked` on the category means a new item lands in the right regime by being filed correctly, rather than by somebody remembering a checkbox |
| 168 | 2026-09-11 | **Pay schemes and the rules that turn a day into money are configuration, not code.** Four situations exist today — upah harian, upah per jam, lembur under the national labour formula, and **undertime** (short hours against a contracted day) — and each is a named rule with its own multiplier, threshold and rounding, editable under IT with the change audited and dated | owner: *skema gaji dan aturan harus customizable*. Every one of these numbers is a policy that changes without the software changing: an overtime multiplier follows regulation, an undertime deduction follows what was agreed with the person, and a rounding rule follows what the workshop has always done. Hard-coding any of them means a policy change arrives as a deployment, and — worse — means nobody can see what the rule *is* without reading TypeScript. Dating the change is not optional: a payslip from March must be recomputable under March's rule, not under today's |
| 167 | 2026-09-11 | **Desain is a module for the drafters, not a folder of files.** It exists to make the drafting team's work faster: what has to be drawn, for which order, which revision is current, what the workshop is building from right now, and what is waiting on an answer | owner: *pastikan modul desain ini dapat mempermudah pekerjaan tim drafter*. The gambar kerja and gambar jadi already live on the product (D150) — a document road, not a workload. What a drafter cannot see today is the queue: which items on which orders still have no drawing, whose turn it is, and whether the revision the workshop is cutting from is the one they last uploaded |
| 166 | 2026-09-11 | **Marketing: `Penjualan` is dropped. The programme that replaces it is *Package* — commission for agents who introduce condo owners wanting an interior renovation — and it gets a session of its own** | owner. The pipeline is not a sales pipeline in the ordinary sense: scrape, enrich, three agent contacts per property, approach the first, fall through to the next until one onboards as a sales representative, then track that agent and the deals they bring. That is an agent-recruitment funnel with an outreach engine attached, and building it inside a generic CRM screen would produce neither. The owner already has an artifact describing it |
| 165 | 2026-09-11 | **HR keeps Berkas 201 and Cuti & Izin; `Compliance` is dropped** | owner: *compliance biar skip, saya sendiri gak paham*. A screen nobody in the business can define is a screen that will hold a number nobody can check. It comes back if and when somebody names what it must show |
| 164 | 2026-09-11 | **No accounts receivable, no client invoicing, and no project profitability in this system.** Clients deal with finance directly; the project's value and its margin are the owner's own figures | owner: *kita tidak handle penagihan ke klien, nilai project dan keuntungan sepenuhnya dari pimpinan*. This settles what `contract_value` is for: reading spend against, and nothing else (Q37). It also means the cost reports stay **cost** reports — materials against materials — and never grow a profit column, which would be a number this system is not entitled to compute |
| 163 | 2026-09-11 | **Tax is the consultant's, not the system's.** No PPN, no PPh, no withholding anywhere in procurement or the ledger | owner: *kita punya konsultan pajak sendiri, mari abaikan dulu*. The consequence for payroll is stated rather than hidden: the payslip keeps saying **bruto**, and Q30 stays open until the consultant says which deductions apply to whom (D140). A tax figure invented here would be wrong in a way that reaches an employee's pocket and a tax return at the same time |
| 162 | 2026-09-11 | **No financial statements, no cash count, no vendor-return document, and no RFQ.** The twelve-month cashflow and the ledger replace a P&L and a balance sheet; a return to a vendor is a WhatsApp message the same hour, and the receipt already records the condition and the credit | owner, answering four at once. Each of these is a module somebody would expect to find and nobody here needs, and the cost of building an unwanted one is not the week — it is the figures it puts on a screen that then have to be explained. What the receipt keeps is what the business actually uses: `DAMAGED` / `WRONG_ITEM` with a photo, and the credit it produces (F27, D98) |
| 161 | 2026-09-11 | **The demo layer survives the swap.** `src/demo` is not deleted when the real backend arrives: one environment flag chooses which client `src/services/*` talks to, and both satisfy the same signatures. The demo stays the guided tour, the offline sandbox and the way a screen is reviewed without a database | it would be thrown away on the grounds that it is scaffolding, and that is the wrong reading of what it is. It is the **only** place in this system where every rule is visible in one file a person can read, and it is what makes a screen demonstrable to the owner on a phone with no connection. It also has one property the real backend can never have: a fixture can be edited to produce the awkward case — the payroll run over a week nobody worked, the partly sawn load — in ten seconds. Keeping it costs one flag and the discipline of not letting the two implementations' signatures drift, which the type system enforces anyway |
| 160 | 2026-09-11 | **When the demo contract and the Postgres schema disagree, the schema wins**, and the TypeScript types are generated from it (`supabase gen types typescript`). Four disagreements are already known and listed as C1–C4 in `phase-2/01-schema.md`; the largest is that a document kind is stored as a **code** (`nota`) with its label in a table, not as the display string the demo carries | two sources of truth for a name is how a field ends up spelled two ways in one database. Generating from the schema makes a rename in SQL a **compile error** in the frontend, which is the cheapest possible way to find out. The display string loses because a stored value must not change when somebody rewords a label — and because sorting, grouping and any future translation all need a stable key underneath the words |
| 159 | 2026-09-11 | **Phase 2 is built in a separate session on its own branch, while this session keeps designing.** The split is by **file ownership**, not by coordination: the build session owns `supabase/**`, `src/lib/api/**` and `docs/plan/phase-2/**`; the design session owns screens, `src/demo/**` and the rest of `docs/plan/`. `src/services/*/contracts.ts` is the one shared surface, and changes to it travel in one direction — the build session writes them down, the design session applies them. Build merges design; design never merges build, until one deliberate pull request at the swap | owner: *buat supaya kita bisa membangunnya di sesi baru, biarkan sesi ini bisa lanjut desain*. Two sessions in one repository is either free or expensive, and which one depends entirely on whether they share files. Ownership by path makes almost every merge trivial; the seam that cannot be split — the contracts — gets a protocol instead of a rule, because it is the one place where a change in either direction is legitimate. Build merges design rather than the reverse because the backend must keep up with the screens, not the screens with the backend: a screen that exists is a requirement, a table that exists is an implementation |
| 158 | 2026-09-11 | **A payroll period can be read without opening a run, and the weeks slide.** `/hrd/payroll/minggu` shows Monday→Sunday for any week — figures derived from the days either way (A3) — with ‹ › to walk, *Minggu ini* to come back, and the week in the address so a reload lands where somebody was. Where a run covers the week it is that run, with its status and its adjustments; where none does, the screen says so and offers to open one | owner: *untuk data gajian mingguan, buat kita bisa geser antar periode gajian*. The workshop is paid weekly, so the question is **what does this week look like**, not *what does run pyr-26-09-06_01 look like* — and a list of run numbers answers the second question only. Because nothing is stored, the week before a run exists is just as computable as the week after, which is what makes walking backwards honest rather than a stub. The run number stays empty on an unopened week on purpose: no document means no approval and no printable payslip, and inventing a number there would make a slip printable for a run that does not exist |
| 157 | 2026-09-11 | **Every table pages — 25 rows by default, and the bar says `1–25 dari 137`.** `DataTable` does it for the twenty screens built on it; `usePaged`/`<Paged>` do it for the lists and grids that are not tables (the attendance grid, the lembur sheets, the timber loads, the verification queue and its history). Footer totals stay **whole-dataset** totals: a page of a ledger is a view, not a subset of the money | owner: *jangan render seluruh data dalam satu halaman, selalu pakai pagination*. Forty people today and a year of purchase orders by next August: the tables grow with the business and nobody reads past the twentieth row anyway. Two things made it worth doing centrally — the row index handed to a renderer stays the row's place in the **whole** list, so a numbered column does not restart at one on every page, and a filter that shortens the list resets to page one rather than leaving somebody staring at an empty page seven. The count is always printed, because a table that silently shows a quarter of the data is worse than a slow one |
| 156 | 2026-09-11 | **A payslip is a card, not a page: as many fit an A4 as actually fit, measured rather than guessed** (five to seven with the week recap, six to eight without). For a daily worker it prints the owner's own sketch — the week across, *masuk · pulang · jam · lembur* down — then hari dibayar, jam lembur, terlambat, and every adjustment with its sentence. Where the grid and the totals disagree the slip **says why**: a day the machine left incomplete is marked `?` and named in words, and overtime the machine saw but nobody approved is stated beside what was paid | owner: *payslip harus bisa hemat, 1 kertas A4 bisa untuk 4-8 atau lebih*, and *bisakah formatnya menampilkan rekap seminggu*. A fixed count is wrong in both directions — a workshop slip with a week grid and two hand-written deductions is half again the height of a salaried one, so eight per sheet either cuts a slip across a page break or wastes a third of the paper; laying them out once at the printed width and reading their real heights costs one render and is exact. The reconciliation lines are the part that matters most: Karjo's slip showed five days of hours and paid 1,5 of them, which without a sentence beside it is the payslip an employee brings to HRD in anger. The hours are on the paper either way — what the slip owes them is the reason |
| 155 | 2026-09-11 | **What HRD adds to or takes off a payslip is a typed, signed, reasoned row — never a calculation.** Five kinds (keterlambatan, SP, selisih periode lalu, kasbon, tambahan), a signed amount, and a **reason that is required** and printed on the slip verbatim. They belong to the **run**, not to the week, and cannot be touched once the run leaves `DRAFT`. Lateness is shown in minutes as evidence beside them; the rupiah is typed by a person (Q41) | owner: the payslip must show *total gaji dikurangi keterlambatan, SP, dan ditambahkan balance atau kekurangan dari gaji periode sebelumnya*. Every one of those is a **decision about one person**, not arithmetic over records: the system does not know what a minute of lateness costs here, what an SP costs, or what was left over last time, and a plausible number invented for any of them is a wage dispute with our fingerprints on it. What the system can insist on is the sentence — *Potongan Rp 100.000* teaches an employee that money disappears, while *SP-1: meninggalkan pekerjaan tanpa izin, 2 September* can be argued with, which is the point. Distinct from the statutory deductions still refused under D140/Q30: those are law and unknown, these are ours and known |
| 154 | 2026-09-11 | **The company's own overtime form is uploaded and read, not retyped into ours.** *FORM LEMBUR KARYAWAN PT TALAHOME* — `NO · NAMA · DESCRIPTION · GAJI · JAM · TTD` — is exported and imported against a sheet. People are matched **by name**, because that is the only identifier the paper carries; a name we do not know is listed back and its row left out, never created. The **GAJI column is paid as written** (`overtime_lines.form_amount`), with any approved hours that carry no figure paid at the ordinary rate; the two are added because they cover different hours | owner: *untuk form lembur, gunakan form ini, kita upload, sistem baca*. The paper already exists, is already signed, and is already the thing leadership approves (D146) — asking a supervisor to retype the night into a different shape is how the two versions start disagreeing. Paying the written figure rather than recomputing it is the same rule as everywhere else money is concerned: the man signed for a number, and a system that quietly pays a different one because its own multiplication came out differently is wrong even when its arithmetic is right. Names, not numbers, means the matching is fuzzy in exactly one direction — we normalise spacing and case, and stop; unknowns are reported so somebody registers the person, which is a conversation, not a silent insert (D143) |
| 152 | 2026-09-11 | **Any request line may name the work order it is for**, picked from the jobs actually open in production — not only the lines a BOM generated | D151 linked the requests the system raised itself; most requests are still typed by a person, and those are the ones that go untraceable. One optional field on the line turns *plywood* into *plywood for the BABY ISLAND tables*, which is what an approver reads and what a projection can be counted against. Optional because stock, office supplies and a broken planer belong to no job, and forcing a job on them would teach people to pick one at random |
| 153 | 2026-09-11 | **Timber is an eighth service (`inventory`), because logs and boards are two different quantities with a saw between them.** A load of logs is measured stick by stick (Ø and length → m³), the boards that come out are measured and counted, and the figure that decides a vendor is **rupiah per cubic metre of board**, never the invoice price. Yield and that price are computed **only over the logs actually sawn**, and the seller's claimed volume is kept beside ours rather than replacing it. Vendors are compared **per species** | the owner asked to count timber from log to board against what each purchase cost per vendor, and the reason it matters is arithmetic nobody can do on an invoice: a vendor at Rp 15,5 juta per log metre whose logs saw out at 45% sells usable wood at Rp 34,6 juta, while one at Rp 18,2 juta sawing out at 61% sells it at Rp 29,6 juta. **The dearer invoice is the cheaper wood**, by five million rupiah a cubic metre, and every one of those numbers is invisible until both volumes are recorded. Partial loads must not set the price: dividing a whole invoice by the boards off half the logs prices the wood half again too high. Two measuring conventions are in use (*bulat* and *persegi*, ~21% apart), so the purchase records which one produced its number (Q39) |
| 93 | 2026-09-11 | **`/accounting/documents` is kept but not developed further** — the owner does not need a document browser. It stays because it is built and costs nothing to leave; nobody should spend another hour on it | owner. The one number on it that matters — files attached to nothing — belongs to the exception inbox (M10) anyway, which is where somebody actually resolves them. Browse-by-file was a filing cabinet nobody opens, and saying so now is cheaper than finding out in month three |
| 90 | 2026-09-11 | **The stored sandbox carries a signature of its shape and reference data**, and is thrown away when it stops matching. The reset says why | a snapshot saved before a change wins silently over the fixtures, so the code and the screen drift apart — which is exactly how a newly added account was invisible (F24). A hand-bumped version number is a thing to forget |
| 84 | 2026-09-11 | **Ledger mutations record what changed, field by field**, and the trail is read from the row itself | owner, for anomaly and fraud detection. "Who touched the ledger this month" is not the question anybody asks; "what happened to *this* row" is, and it is asked while looking at it. A void now carries amount-before and amount-after, because a void is the one action that makes money vanish from a total |
| 85 | 2026-09-11 | **No document, no ledger row.** At least one nota, transfer proof or photo of what arrived; a delivery note or a PO may come with it but cannot stand alone. Files are uploaded and linked in the same act as the row | owner. The old way let a number be typed and the paperwork follow "later", and later is where the unexplained rows live. The rule already governs receiving (A15) — this extends it to money |
| 86 | 2026-09-11 | **A purchase row carries quantity, unit price and vendor.** Non-purchase types (payroll, utilities, bank charges) do not | owner. An amount on its own cannot be compared to the last time we bought the same thing, which is the only way a price is ever found to be wrong. The line total must equal the transaction, and the ledger will not guess which of the two is right |
| 87 | 2026-09-11 | **Cash position across five accounts at the top of the ledger** — PETTY CASH, BNI 325, BCA 271, JAGO, BCA 064 — with the leadership account's balance shown only to `approve_funds` holders, and marked locked rather than hidden | owner. A blank where an account should be reads as a bug; "leadership only" reads as a rule |
| 88 | 2026-09-11 | **The ledger has no "allocated" figure.** It is money that moved, never a budget. What it shows instead is whether a purchase names a request at all | owner. Splitting a spent amount into allocated and unallocated made a record of the past look like a plan for the future |
| 89 | 2026-09-11 | **Void sits behind a deliberate step**, not beside the ordinary actions | owner. It is the correction for a row that should never have existed, and one accidental click away from a row somebody was only reading. The button stays — hidden actions are worse than dangerous ones — but it takes a sentence to reach |
| 82 | 2026-09-11 | **A round is funded in as many transfers as it takes.** The record is a list of instalments, each with its own proof and its own ledger row; the round shows what has come in and what is still short | owner. Half on Monday and the rest when a client pays is the ordinary case. A single `transferred_amount` column could only hold the last one, or a total nobody could take apart |
| 83 | 2026-09-11 | **Unallocated money is flagged only on transaction types that are purchases.** Payroll, utilities and bank charges are money leaving for reasons nobody raises a request for | otherwise the ledger marks two thirds of a normal month as a problem, and people learn to ignore the flag — which is worse than not having it. `is_purchase` was already in the type table |
| 80 | 2026-09-11 | **A round cannot be marked funded without the transfer proof.** `transferRound` refuses (422) without an attachment, and the round keeps its id | owner. "Transferred" is a claim about the bank, and the sheet's version of that claim was a tick somebody typed — which is how a round could read funded while the money was still in the leadership account. The same rule already governs receiving: no photo, no receipt (A15) |
| 81 | 2026-09-11 | **Money in has two roads.** Either accounting makes the transfer and uploads the receipt (both ledger legs written here), or leadership transfers from a phone and drops the proof in chat — where it waits in the inbox until somebody books it as an IN transaction, and the round then points at money already in the ledger | owner. The second is what actually happens most weeks. Inbox rows now carry `money_direction`, because a transfer proof is resolved by a different person for a different reason than somebody who bought first |
| 78 | 2026-09-11 | **Approving a round is `approve_funds`; recording its transfer is `post_ledger`.** Closing is `approve_funds` again | the funds decision is the approval. The transfer is the bookkeeping that follows it, and it is the same act as writing the two ledger legs — one person should not need two authorities to record one fact |
| 79 | 2026-09-11 | **A transfer is written as two ledger legs by the screen**, out of the leadership account and into BCA 271, then handed to `transferRound` | a transfer between our own accounts is two rows. Recording it once, on whichever side somebody was looking at, is how the two accounts came to disagree by exactly the amount that moved. The screen composes both services rather than either reaching into the other (ADR-004) |
| 75 | 2026-09-11 | **A line's amount is quantity × price when it has both, and an editable figure when it does not.** The edit form carries all three; typing the amount leaves quantity and price alone and says so | owner — the price could not be changed on a service line at all, and saving one recomputed its amount from a missing quantity and zeroed it. Plenty of real lines have no quantity: a service, a delivery charge, a lump sum |
| 76 | 2026-09-11 | **Approval is no longer capped at what was requested.** Quantity and amount may be raised as well as cut | owner. **Supersedes A8's application to approval.** The old rule assumed the request was always the higher number, but a vendor raises a price between the request and the meeting, and a leader approving above it is deciding, not erring. The gap between requested and approved is already carried and reported (D54) |
| 77 | 2026-09-11 | **Ticking on the meeting board picks, it does not decide.** Nothing is written until one confirm above the lists, which carries the count and the total | owner. A meeting goes down the list, changes its mind twice, and wants to see what it just committed to before committing it. The confirm is *Approve* for the authority holder and *Send to the approver on Chat* for everybody else |
| 73 | 2026-09-11 | **An item can be added from the meeting itself**, in one act: a real purchase request with its own number, submitted straight away | owner. Somebody says "we also need thinner" and it has to be on the list before the conversation moves on. A note on paper never comes back |
| 74 | 2026-09-11 | **The meeting has its own board** (`/procurement/meeting`): waiting for a decision, approved and unpaid, the BCA 271 balance, and the transfer needed to cover it. The requests board goes back to being the working surface — asking, correcting, documenting, paying | owner. Same lines, two readings. Approval controls in the middle of somebody's working day are noise; a working board in the middle of a meeting is noise the other way |
| 70 | 2026-09-11 | **Approval is asked for as a batch, not a card per line.** One send carries the list and three totals: asked for, approved so far, and what has to be paid — with the BCA 271 balance beside them. "Approve the rest as asked" covers what is left | owner. Fifteen separate cards ask the approver to add fifteen numbers in their head to know what they just committed to, which is how people stop reading the fifteenth |
| 71 | 2026-09-11 | **The board is grouped into three piles: waiting for approval, approved and waiting for payment, finished.** Each states its own subtotal | owner. Not-approved and approved are different kinds of work — one needs a decision, the other needs cash — and one mixed table could show neither total. A line paid without a yes sits with the undecided, because the money is gone but the decision is still owed |
| 72 | 2026-09-11 | **Chat tokens are random and unique, never derived from a document number** | a token is a capability: the card carries it back and it is what identifies the request. A predictable one lets anybody who can guess a number answer somebody else's list, and two sends deriving the same token answer each other's |
| 64 | 2026-09-11 | **`instructions` and `remark` are leadership's two optional fields**, kept in `line_notes` rather than as columns on the approval | owner. They are different acts: instructions is something to DO, remark is for the record. And the useful moment for "get another quote" is on a line nobody has decided yet, which a column on the approval row could not hold |
| 65 | 2026-09-11 | **Approval can cut the quantity, not only the amount.** Changing the quantity recomputes the amount at the unit price | owner. "Approve 40 of the 60 litres" is the decision actually taken; making somebody do that multiplication by hand is how an approval ends up disagreeing with itself |
| 66 | 2026-09-11 | **A line is editable until it is approved**, not merely while it is a draft. Three doors close it: approval, payment, removal. The old values go into the audit row | owner. **Supersedes D51.** Spotting a wrong quantity an hour after submitting should not cost the line its number and its place in the queue |
| 67 | 2026-09-11 | ~~**One board, not two.**~~ **Narrowed by D74**: the separate *approval queue* stays deleted, but the leadership meeting got its own screen, because it asks a different question of the same lines. What is not coming back is a second list of what is outstanding. Approving happens on the requests board, inline per row; the separate approval queue is deleted | owner. Approving is not a second subject — it is a column on the same list. Two screens meant two lists that could disagree about what is outstanding |
| 68 | 2026-09-11 | **The board shows the BCA 271 balance against what is approved and unpaid**, and names the top-up needed | owner. At the moment of approving, the useful number is not "how much did we approve" but "how much has to be in the account before any of it can move" |
| 69 | 2026-09-11 | **Approval is asked for in Google Chat and answered there.** The identity on the record comes from the chat platform's authentication, never from the browser session; anyone in procurement may ask, only the addressee may answer | owner. The meeting runs on one laptop, usually not the approver's. An approval trail naming the wrong person is worse than none, because it looks authoritative |
| 59 | 2026-09-11 | **A liquidation report belongs to accounting (M12), not procurement.** Money in against where it went: by type, project, vendor, and — the split nothing today can show — spending with a PR line behind it against spending without one | owner. It is a read over the ledger, so it costs nothing to defer and would cost a screen to build twice |
| 53 | 2026-09-11 | **A payment posted from a line IS the ledger entry.** One act writes the transaction, the allocation to the line and the document link; the ledger row carries the PR line number in its description | owner. Two people typing the same amount into two systems is how they disagree. The `source_ref` (`pr-line:<line>:<date>:<amount>`) makes a retry a no-op rather than a second payment |
| 54 | 2026-09-11 | **Requested → approved is a decision; approved → paid is a variance.** Only the second is reported as a difference | the CEO cutting an amount is the system working. Reporting it as an exception would bury the real one |
| 55 | 2026-09-11 | **The application never records whose fault a difference was.** It records what kind of difference it was, from a closed list, and who said so | it cannot know, and a field that guesses gets believed. One gap is unknowable; twelve tagged the same way against one vendor is a fact |
| 56 | 2026-09-11 | **An underpayment explained by anything except "paid in parts" closes the line**, writing the `line_settlements` row with the explanation as its reason | A12 wanted a shortfall closed by a named human reason. That is the same statement, so it is one act, not two |
| 57 | 2026-09-11 | **A line with an unexplained material difference stays on the board even when COMPLETED**, and is badged `needs an explanation` | a line dropping off because the goods arrived is exactly how an overpayment stops being anyone's problem |
| 58 | 2026-09-11 | **Paying is offered only on an approved line.** Money moving before a yes is recorded when it happens, but never offered as an ordinary button | the paid-unapproved corner should be reachable by accident in the world, not by a button in the app |
| 45 | 2026-09-11 | **The audit seam is built now; the IT module is not.** Every mutation writes its audit row in the same transaction as the change — already true of all 23 writes. The IT screens, read-access logging and retention wait | the seam is the expensive half to retrofit; tables and screens are not. Building the module now would delay procurement without making the trail any more complete |
| 46 | 2026-09-11 | **Session events are recorded from M2** (`entity: "session"`, action `sign_in`) | a trail that cannot say who was signed in cannot answer the first question asked of it, and it is a handful of call sites in one service |
| 47 | 2026-09-11 | **Read-access logging is deliberately deferred**, with retention as an open question for the owner | it is the only trail that grows without bound and costs something per request; it is middleware, not schema, so it stays cheap to add |
| 43 | 2026-09-11 | **Vendors carry a named contact**: `pic_name`, `pic_phone`, plus the office line, address, a second bank account and declared categories | owner. "Call Toko Amplas" is not an instruction anyone can follow |
| 44 | 2026-09-11 | **Sourcing is derived, not declared.** `purchaseFacts()` answers both "what do we buy here" and "where do we buy this" from one computation; the declared category list is a fallback for vendors with no history | a maintained field goes stale the day a vendor changes what it carries. History cannot |
| 40 | 2026-09-11 | **Brand is `OPS TALALIVING` / `PT TALAHOME`** | owner. Still one file, `src/lib/brand.ts`; the colour scale remains a placeholder |
| 41 | 2026-09-11 | **A merged vendor's row is kept, marked `merged_into`, never deleted or repointed** | with real foreign keys (D4), repointing would rewrite what a past transaction says. Keeping the row means history does not move when somebody corrects a name later |
| 42 | 2026-09-11 | **`<Loaded>` / `<SourceBadge>` moved from M4 to M3** | both M3 screens needed failure handling the day they were written; retrofitting two screens later is worse than building it once |
| 38 | 2026-09-11 | **Routes are English**: `/signin`, `/no-access` — not `/masuk`, `/tanpa-akses` | a URL is interface. An English interface with Indonesian routes reads as a half-finished translation (D34) |
| 39 | 2026-09-11 | **Decision verbs are not in the module catalogue.** `approve`, `post`, `close` were removed from `PERMISSION_CATALOG`; only access verbs remain | D24 in code. Leaving `procurement.approve` in the catalogue would have let a procurement admin approve goods without the authority — the exact fusion the split exists to prevent |
| 37 | 2026-09-11 | **Vercel's Production Branch points at `claude/serene-euler-eq2qef`**, not `main` | owner. Phase 1's whole output lives on one branch; pointing production at it beats merging an unfinished phase into `main` just to see it. `main` keeps the original shell until Phase 2 |
| 36 | 2026-09-11 | **`en-US` number grouping**, set once as `LOCALE` in `src/lib/format.ts` | an English interface showing `Rp 18.900.000` is genuinely ambiguous — an English reader sees eighteen point nine. Mixing an English UI with Indonesian grouping is the one combination misreadable as a number a thousand times smaller. One line to change back |

---

**Q40 — answered 2026-09-12.** *Should sawn boards be drawn down as
production consumes them?* Yes: the owner asked for **pencatatan
penggunaan**, and the board list is now a movement ledger (D203). The old
default — *not tracked, and the screen says so* — was right for exactly as
long as nobody was writing down what was taken off the rack. What made it
cheap to answer is that the shape was already there twice: the material
stock (D170) and the BOM's expectation of what a run should consume.

**Q22 — answered in full, 2026-09-12.** *How long is the access log kept, and
who may read it?* Both halves are now the owner's own: **30 days of detail and
six months of daily recap** (D188), and **only IT and leadership may open the
module** (D190). The default I had taken in the meantime — `it.read` plus a
person seeing their own row — is gone: the self-view half was never built, and
it is not being built now, because the owner named two groups and a person
reading their own log is neither. If that turns out to be wanted it is one
branch in `listActivity`; it is not something to assume.

## Open questions — with the default we take until told otherwise

These are the owner's own unanswered questions from §10.2 of the rekap, plus
the ones this plan raised. **Phase 1 is the cheapest possible place to answer
them**: the demo can show two versions of a screen and let the owner point.

| # | Question | Default we take | Cost of changing later |
|---|---|---|---|
| Q29 | **What does a signature on the vendor's PDF actually need to do — prove who approved it, or let the vendor check the order is real?** A scanned signature image, a cryptographic signature, or a QR code resolving to the order on our side | **Not defaulted; the answers are not interchangeable.** A scanned signature is what suppliers expect and what a photocopier reproduces perfectly, which means it proves nothing and everybody accepts it. A cryptographic signature proves a great deal and no supplier in this trade can verify one. A **QR pointing at a read-only page for that PO** is the only one that is both checkable and useful — the vendor scans it and sees the live order, which also catches an amended PO being presented as the original. It needs a public read route and a token, so it is a Phase 2 decision, not a Phase 1 one | low to defer, moderate once the first vendor disputes a printed order |
| Q43 | **What does stock that leaves the rack cost — FIFO, weighted average, or standard?** The three give different numbers the moment a price moves, and every one of them changes what a work order appears to have consumed | **Not defaulted, and not computed.** The rack is valued at the weighted average of what is **on** it, which is the question actually being asked today; the cost of an issue is left blank rather than picked. Answering it makes the BOM's *proyeksi vs aktual* comparison sharper — it does not block it, because that comparison is against purchase prices | low now; high once a year of issues has been valued one way and somebody wants the other |
| Q41 | **What does a minute of lateness cost, and what does an SP cost?** The timesheet knows the minutes; nothing knows the money | **Not defaulted, and payroll will not invent it.** The slip prints the minutes as evidence and the rupiah is typed by a person with a reason attached (D155). `LATE_AFTER_MINUTES` (08:00) is a policy constant in one place, so the threshold is a one-line change once somebody states it | low — the row already exists, what is missing is the rule behind the number |
| Q30 | **Which statutory deductions apply to these people, and at what rate?** BPJS Kesehatan, BPJS Ketenagakerjaan (JHT, JP, JKK, JKM), PPh 21 | **Not defaulted, and payroll refuses to invent them.** The rates are public; what is not public is which of these fourteen people are enrolled, who pays which half here, and from what date. Needed before a payslip can be given to anybody | high — it is the difference between a payslip and a draft |
| Q32 | **Is payroll weekly, monthly, or both at once?** The workshop is paid by the day and staff by the month, and the calendar already assumes a weekly payroll run | **Both, and the period lives on the run rather than in the code.** A run covers whatever dates it is opened for, so a weekly workshop run and a monthly staff run can both exist. If that is wrong the fix is a filter on who a run includes, not a new model | low |
| Q35 | **Are these the seven stages a piece actually goes through?** Potong · Serut/bentuk · Rakit · Amplas · Finishing · QC · Packing | **Seeded as data, not typed by anybody**, so every screen says the same thing and the order is checkable — a piece cannot be sanded before it is cut. The list itself is our reading of a furniture workshop and is the part most likely to be wrong in detail | **A seed edit.** Adding, renaming or reordering a stage is one line of data, not a schema change — which is exactly why it is data. Progress already recorded keeps its stage code |
| Q36 | **Should a bill of material be versioned, and pinned to the work order that used it?** Today a BOM is current-state: change it and every past order reads as if it always had the new components | **Current-state, with every change audited.** Who changed which component, when, and from what is recorded — so the history exists even though the BOM itself has one version | **A revision table plus a `bom_rev` on the work order.** Cheap now, expensive once a year of orders has to be back-filled with the revision they were actually built from |
| Q37 | **Where does a quotation live, and is `contract_value` it?** The project carries an agreed order value; it is not a quotation, not an invoice, and there is no document behind it | **Not defaulted, and nothing pretends to quote.** `contract_value` is what the project is worth for reading spend against, and the screen says exactly that | **A sales module.** A quotation is a document with lines, versions and an expiry, and inventing half of one inside the project record would make the other half harder to add |
| Q38 | **What does an hour of workshop time cost, and how many hours does each product take?** The BOM prices materials and stops there | **Materials only, said plainly on the screen.** Labour is where a made-up number does the most damage: it would flow straight into a quoted price | **One field per product plus a rate.** The rate is the hard part — it is not the daily wage divided by eight, and getting it from the owner is a conversation, not a lookup |
| Q39 | **Which kubikasi convention does the business buy on — bulat (π/4 × d² × p) or persegi (d² × p)?** They differ by about 21%, so a vendor quoting one and a buyer assuming the other disagree on every load | **Recorded per purchase, defaulting to `bulat`**, and the screen names which was used. Nothing converts silently between them | **A field that already exists.** The cost is only in the loads already entered under the wrong convention, which can be re-marked |
| Q24 | **The calendar now claims the money runs out in November. Who owns the estimates behind that claim — accounting, or leadership?** Whoever types them owns a number the business will act on | **Accounting types them, leadership reads them.** The estimates are seeded from what actually happened, and every cell shows actual beside planned, so a wrong estimate is visible rather than argued about. Change it if leadership wants to set the figures directly | low — it is a permission, not a model |
| Q25 | **When the plan says November, which lever is the intended one** — bigger transfers in, or fewer commitments out? | **Not defaulted.** The app states the shortfall, the month, and the day; choosing between "put more in" and "buy less" is the owner's call, and an app that implied one would be making a business decision in a tooltip | none technically, high in what it implies |
| Q26 | **Who puts the expected date on a payment term** — procurement when the order is placed, or accounting when the month is planned? | **Procurement, at order time**, defaulting to blank. They are the ones talking to the vendor about when it lands. Accounting can override on the calendar, and the override is a row with a reason | low |
| Q27 | **Rp 21.720.000 of purchases went out with no approved line or order behind them. Is that a normal rate, or a target to drive to zero?** | **Shown, not judged.** The liquidation report names it per transfer and the inbox exists to convert it into a decision after the fact. No threshold, no alert, until somebody says what "too much" is — a number invented here would become a target nobody agreed | low |
| Q5 | BOM: per product or per order? Layered or flat? Are PRs made *from* a BOM? | **Not modelled at all.** The standing instruction is "do not invent it" | n/a — deliberately absent |
| Q7 | How is `PAID_UNAPPROVED` handled — recover or write off? | **Neither automatically.** It is terminal, flagged to leadership, never deleted. The policy is the owner's | low |
| Q21 | **When multi-language arrives, which languages and who chooses?** | **Not designed now.** `LOCALE` is a single constant that becomes a session value; UI strings are not yet extracted into a message catalogue, and doing that before there is a second language is work with no reader | low now, moderate once screens multiply |
| Q10 | What is transaction type `EJO` — 77 transactions, Rp529 M, unclassified? | **Carried as-is, unclassified, and shown**. Never quietly folded into `OTHERS` | low |
| Q13 | What is the app actually called, and what is the brand colour? | **Placeholders stay** (`MANUFAKTUR OS`, `#2f6b52`), isolated in `src/lib/brand.ts` and the `brand` scale. Changing the colour means deriving the whole 50–950 scale, plus `BRAND` in `charts.tsx` and `themeColor` in `layout.tsx` | trivial while it stays in one place |

## Answering one from a phone

Reply with the number and the answer. For example: *"Q18: grant
`approve_goods` to the operations director as well, so lines do not wait when
the CEO travels."* The next session moves the row into Decisions, notes the
date, and changes the code.

## Facts to record here as they appear

| Fact | Value |
|---|---|
| Vercel project URL | *(record on D1)* |
| Vercel preview URL pattern | *(record on D1)* |
| Custom domain | *(not configured — Q12)* |
| Supabase project ref | *(Phase 2)* |
